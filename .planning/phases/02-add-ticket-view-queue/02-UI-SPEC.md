---
phase: 2
slug: add-ticket-view-queue
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-22
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — plain global CSS, no component/UI library (carried forward from Phase 1; per CLAUDE.md "What NOT to Use": no Tailwind/shadcn/CSS framework for this project) |
| Preset | not applicable |
| Component library | none — native HTML elements only (`button`, `ul`, `li`, `section`) |
| Icon library | none — no icons in this phase (text-only "Agregar turno" button and "Turno N" chips; no icon usage anywhere in v1 scope per REQUIREMENTS.md) |
| Font | System font stack — `-apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif` (unchanged from Phase 1, no web font) |

**shadcn gate note:** Not re-run this phase — `components.json` confirmed absent (verified via `ls`), and CLAUDE.md still mandates "single global CSS file, no CSS framework" (Phase 1 D-02, unchanged). This gate remains not applicable for this codebase. No new dependencies introduced (RESEARCH.md confirms zero `npm install` needed for Phase 2).

---

## Spacing Scale

Declared values (must be multiples of 4) — unchanged from Phase 1, reused here:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Reserved — not used directly in this phase's new elements |
| sm | 8px | Gap between ticket chips in the wrapping row (`.ticket-list` `gap`) |
| md | 16px | Padding inside the queue strip (already established); not changed by this phase |
| lg | 24px | Section padding (queue-strip padding); margin below the "Agregar turno" button before the queue strip |
| xl | 32px | Outer page padding (unchanged from Phase 1) |
| 2xl | 48px | Not used this phase |
| 3xl | 64px | Not used this phase |

Exceptions: none. The "Agregar turno" button uses standard padding `12px 20px` (not on the 8-pt scale on the horizontal axis, matching common button padding conventions) — this is the one explicitly-allowed button-padding exception per common practice; vertical/gap spacing around it stays on-scale (`24px` margin-bottom per D-07 button placement).

---

## Typography

Unchanged from Phase 1 — exactly 4 sizes, exactly 2 weights, reused as-is. No new size/weight introduced for ticket chips or the button (both use Body or Label scale):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 14px | 400 (regular) | 1.5 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.2 |

Notes:
- **"Agregar turno" button label:** Body size (16px), weight 600 (semibold) — semibold reused here for emphasis on the primary interactive element, consistent with Heading/Display weight, not introducing a third weight.
- **Ticket chip text ("Turno N"):** Body size (16px), weight 500 is NOT used (would be a third weight) — use weight 400 (regular) per the existing 2-weight contract. RESEARCH.md's code example used `font-weight: 500` for chips; this is overridden here to stay within the locked 2-weight contract (400/600 only). Executor must use 400, not 500.
- Display (28px) remains reserved for the page title "Turnero" only — ticket numbers do NOT use Display size in this phase (Phase 7 will introduce a dedicated distance-readable size for ventanilla current-ticket displays; queue chips in Phase 2 are small/Body-sized by design, per D-05 "small rounded badge").

---

## Color

Unchanged baseline from Phase 1, extended with one new interactive-state addition (button hover/active) — still grayscale/neutral, no semantic color yet (no destructive actions until Phase 3):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #FFFFFF | Page background; ticket chip background (chips sit on white, distinct from the queue-strip's gray container — per RESEARCH.md A2) |
| Secondary (30%) | #F1F3F5 | Queue strip background, ventanillas grid placeholder background (unchanged); "Agregar turno" button background (reuses existing secondary-background convention, per CONTEXT.md discretion note) |
| Accent (10%) | #1F2933 (dark neutral) | Page title, section headings, "Agregar turno" button text, ticket chip text — text-only accent, no new hue introduced |
| Destructive | not applicable this phase | No destructive actions exist until Phase 3 (WINDOW-02 remove-ventanilla blocking) |

Accent reserved for: page title text, section headings ("Cola"/"Ventanillas"), the "Agregar turno" button's text label, and ticket chip text. Not used as a background fill anywhere — backgrounds stay within Dominant/Secondary only. No new color is introduced this phase; the button and chips reuse the exact two neutrals already locked in Phase 1's UI-SPEC.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "Agregar turno" (locked, D-07 in 02-CONTEXT.md) — placed above the queue strip, between `<h1>` and `.queue-strip` |
| Ticket label format | "Turno {N}" (locked, D-04) — e.g. "Turno 5"; applies to every ticket chip rendered in the queue, and must remain the same format wherever tickets are shown in future phases unless explicitly revisited |
| Empty state heading | "Cola" (unchanged section heading from Phase 1) |
| Empty state body | "Próximos turnos aparecerán aquí" (locked, D-06) — shown both on first load before any ticket exists AND whenever the queue empties again after tickets are added/removed; one string covers both cases, no separate "no hay turnos en espera" copy in this phase |
| Error state | not applicable — this phase has no error conditions (button click has no failure mode; no network, no validation, no user input beyond the click itself per RESEARCH.md Security Domain) |
| Destructive confirmation | not applicable — no destructive actions exist in this phase (removal/call logic arrives in Phases 3-4) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized for this project |
| third-party | none | not applicable |

---

## Layout Contract (Phase-specific addition)

Captures D-05 and D-07 from 02-CONTEXT.md so the executor has an unambiguous structural reference. Extends Phase 1's layout contract; does not replace it.

| Region | Position | Structure |
|--------|----------|-----------|
| "Agregar turno" button | Between `<h1>` page title and `.queue-strip` section (D-07) | `<button type="button" className="add-ticket-button">`, Body size (16px) / weight 600, Secondary background (#F1F3F5), Accent text (#1F2933), `border-radius: 6px`, padding `12px 20px`, `margin: 0 0 24px` (24px gap below before queue strip), `cursor: pointer`, no border |
| Queue strip — ticket list | Inside existing `.queue-strip` section, replacing the placeholder `<p>` when `queue.length > 0` | `<ul className="ticket-list">` with `display: flex; flex-wrap: wrap; gap: 8px` (D-05 — horizontal wrapping row, never sideways-scrolling, never vertical stack); each `<li className="ticket-chip" key={ticket.id}>` renders "Turno {ticket.number}" |
| Ticket chip | Inside `.ticket-list` | `border-radius: 999px` (fully rounded "badge" shape per D-05), `padding: 6px 14px`, Dominant background (#FFFFFF, distinct from queue-strip's #F1F3F5 container), Accent text (#1F2933), Body size (16px) / weight 400, no border |
| Queue strip — empty state | Inside `.queue-strip`, replacing the ticket list when `queue.length === 0` | Unchanged from Phase 1: `<p>` "Próximos turnos aparecerán aquí", Body size (16px) / weight 400 |

**Conditional render rule:** The queue strip body is a single conditional — `queue.length === 0` renders the existing placeholder `<p>`; otherwise renders `<ul className="ticket-list">`. Both live in the same `.queue-strip` container; no layout shift to the container itself, only its inner content swaps.

**Key usage:** Ticket chips must use `key={ticket.id}` (the ticket's stable identifier), never `key={index}` — required for correct DOM reuse once Phase 4 removes tickets from the front of the array (per RESEARCH.md Anti-Patterns).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
