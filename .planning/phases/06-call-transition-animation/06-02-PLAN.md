---
phase: 06-call-transition-animation
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/index.css
  - src/App.tsx
autonomous: false
requirements:
  - FEEDBACK-02

must_haves:
  truths:
    - "User sees an amber (#fbbf24) flash animation on the ticket number text when a ventanilla calls a ticket"
    - "Animation scope is limited to the <p> ticket element — the card border, label, and buttons are not animated (per D-03)"
    - "Animation restarts from full amber on rapid successive calls without getting stuck (per D-05)"
    - "No animation fires on page load — all ventanillas start with currentTicket: null so flash class is never applied at mount"
    - "All 26 tests pass: 23 prior tests + 3 FEEDBACK-02 tests added in Plan 06-01"
  artifacts:
    - path: src/index.css
      provides: "@keyframes ticket-flash rule + .ventanilla-ticket-flash modifier class"
      contains: "@keyframes ticket-flash"
    - path: src/App.tsx
      provides: "key prop + conditional flash className on VentanillaCard <p> element"
      contains: "ventanilla-ticket-flash"
  key_links:
    - from: "src/App.tsx VentanillaCard <p> key prop"
      to: "React reconciler remount trigger"
      via: "key={ventanilla.currentTicket?.id ?? 'empty'}"
      pattern: "currentTicket\\?\\.id"
    - from: "src/App.tsx VentanillaCard <p> className"
      to: "src/index.css .ventanilla-ticket-flash"
      via: "conditional className: 'ventanilla-ticket ventanilla-ticket-flash'"
      pattern: "ventanilla-ticket-flash"
---

## Phase Goal

**As a** clinic staff member, **I want to** see a brief visual animation on a ventanilla's ticket display when a new ticket is called, **so that** I can instantly spot which window just changed without scanning the entire screen.

<objective>
Implement the FEEDBACK-02 ticket-flash animation in two source files: add @keyframes ticket-flash and .ventanilla-ticket-flash to src/index.css, and update the `<p className="ventanilla-ticket">` element in VentanillaCard (src/App.tsx) to use key={ventanilla.currentTicket?.id ?? 'empty'} and conditional className applying ventanilla-ticket-flash only when currentTicket is non-null. After both source changes, the 3 FEEDBACK-02 tests from Plan 06-01 must turn GREEN, and a visual checkpoint confirms the amber flash renders correctly in the browser.

Purpose: Deliver the visual transition animation so users get immediate confirmation of which ventanilla just changed.
Output: Updated src/index.css (8 lines added), updated src/App.tsx (5 lines replaced), visual checkpoint approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-call-transition-animation/06-CONTEXT.md
@.planning/phases/06-call-transition-animation/06-UI-SPEC.md
@.planning/phases/06-call-transition-animation/06-PATTERNS.md
@.planning/phases/06-call-transition-animation/06-01-SUMMARY.md
</context>

<interfaces>
From src/index.css (lines 117-122) — .ventanilla-ticket block that precedes the insertion point:

  .ventanilla-ticket {
    font-size: 16px;
    font-weight: 400;
    color: #1f2933;
    margin: 0;
  }

Insertion point: immediately after the closing brace of .ventanilla-ticket (after line 122),
before the .ventanilla-remove rule (which begins at line 124).

From src/App.tsx (lines 64-68) — current <p> element to replace:

  <p className="ventanilla-ticket">
    {ventanilla.currentTicket === null
      ? 'sin turno'
      : `Turno ${ventanilla.currentTicket.number}`}
  </p>

Replacement adds key prop and conditional className; inner text content is UNCHANGED.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add @keyframes ticket-flash and .ventanilla-ticket-flash to index.css</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css — read in full to confirm line numbers before editing. The .ventanilla-ticket block ends at line 122. The .ventanilla-remove block begins at line 124. Insert the two new CSS blocks between these (after line 122, before line 124).
    .planning/phases/06-call-transition-animation/06-UI-SPEC.md — section "Animation Contract — @keyframes Rule" and "CSS Class" contain the verbatim CSS to add.
  </read_first>
  <action>
    Insert two CSS rule blocks in src/index.css, between the .ventanilla-ticket closing brace (after line 122) and the .ventanilla-remove rule (line 124). Add a blank line before the first new rule so the inserted block is visually separated from .ventanilla-ticket.

    First block: an @keyframes at-rule named ticket-flash with two keyframe selectors — from sets background-color to #fbbf24, to sets background-color to transparent. No vendor-prefixed version (@-webkit-keyframes) is needed.

    Second block: a class selector .ventanilla-ticket-flash with exactly two properties: animation shorthand set to ticket-flash 600ms ease-out forwards, and border-radius set to 4px. border-radius prevents the amber highlight from appearing as a hard-edged rectangle per UI-SPEC. Do NOT add this class's properties into .ventanilla-ticket itself — they belong on the modifier class only.

    The .ventanilla-ticket rule (lines 117-122) must remain completely unchanged.
  </action>
  <verify>
    <automated>grep -c "ticket-flash" src/index.css</automated>
    Output must be at least 3 — the name appears in: @keyframes ticket-flash, animation shorthand inside .ventanilla-ticket-flash, and .ventanilla-ticket-flash selector itself.
  </verify>
  <acceptance_criteria>
    - src/index.css contains the string "@keyframes ticket-flash {"
    - src/index.css contains "background-color: #fbbf24;"
    - src/index.css contains "background-color: transparent;"
    - src/index.css contains ".ventanilla-ticket-flash {"
    - src/index.css contains "animation: ticket-flash 600ms ease-out forwards;"
    - src/index.css contains "border-radius: 4px;" inside the .ventanilla-ticket-flash block
    - The .ventanilla-ticket block (font-size/font-weight/color/margin) is unchanged
    - grep -c "ticket-flash" src/index.css outputs 3 or more
  </acceptance_criteria>
  <done>
    src/index.css has @keyframes ticket-flash and .ventanilla-ticket-flash inserted after .ventanilla-ticket. No other CSS rules were modified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update VentanillaCard <p> in App.tsx with key prop and conditional flash class</name>
  <files>src/App.tsx</files>
  <read_first>
    src/App.tsx — read in full to confirm the exact <p className="ventanilla-ticket"> element at lines 64-68 before editing. Verify no other <p className="ventanilla-ticket"> elements exist elsewhere in the file that also need updating.
    .planning/phases/06-call-transition-animation/06-PATTERNS.md — section "src/App.tsx — Replacement JSX" contains the exact replacement JSX verbatim. The "Key implementation notes" sub-section explains why ?? 'empty' is mandatory.
  </read_first>
  <behavior>
    - When ventanilla.currentTicket is { id: 5, number: 5 }: the <p> has both 'ventanilla-ticket' and 'ventanilla-ticket-flash' classes, and key is 5
    - When ventanilla.currentTicket is null: the <p> has only 'ventanilla-ticket' class, and key is 'empty'
    - When currentTicket changes from { id: 10 } to { id: 11 }: key changes from 10 to 11, React unmounts and remounts the <p>, animation restarts from frame 0
  </behavior>
  <action>
    In src/App.tsx, replace the <p className="ventanilla-ticket"> element at lines 64-68 (VentanillaCard return) with a new version. The replacement adds two new JSX attributes and keeps the inner text content identical.

    New key prop value: {ventanilla.currentTicket?.id ?? 'empty'} — the fallback must be the literal string 'empty' (not null, not undefined, not 0). The optional-chaining ?.id returns number | undefined; the ?? 'empty' converts undefined to 'empty', giving React a valid number | string key. Never substitute ?? null or ?? undefined — React silently falls back to index reconciliation.

    New className value: a ternary expression. When ventanilla.currentTicket !== null, the className string is 'ventanilla-ticket ventanilla-ticket-flash' (two class names separated by a space). When ventanilla.currentTicket is null, the className string is 'ventanilla-ticket' (base class only, no flash). This implements D-07: no flash when currentTicket is null.

    Inner text content is unchanged from the existing code: the ternary renders 'sin turno' when currentTicket is null, or the template literal `Turno ${ventanilla.currentTicket.number}` when non-null.

    Do NOT apply key prop or flash class to the .ventanilla-card div, the <h3 className="ventanilla-label">, or either button element (per D-03 — animation scope is the <p> only). Do NOT add any new imports. Do NOT change the VentanillaCard props interface. No other lines in App.tsx are modified.
  </action>
  <verify>
    <automated>npm test</automated>
    Must EXIT ZERO. All 26 tests pass: the 23 previously-passing tests plus the 3 new FEEDBACK-02 tests from Plan 06-01 that were RED are now GREEN.
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains: key={ventanilla.currentTicket?.id ?? 'empty'}
    - src/App.tsx contains: 'ventanilla-ticket ventanilla-ticket-flash'
    - src/App.tsx contains: 'ventanilla-ticket' (base-class-only string in the else branch of the ternary)
    - src/App.tsx does NOT contain a standalone `<p className="ventanilla-ticket">` without a key prop (the old form is fully replaced)
    - The .ventanilla-card div, h3.ventanilla-label, and both buttons have NO key prop change and NO flash class added
    - npm test exits 0 with all tests passing (output shows 0 failures, 26 tests)
  </acceptance_criteria>
  <done>
    App.tsx VentanillaCard <p> element has key prop and conditional flash className. npm test exits 0. FEEDBACK-02 tests (3) are GREEN. All 26 tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual checkpoint — confirm animation renders in browser</name>
  <what-built>
    CSS @keyframes ticket-flash animation (600ms ease-out, #fbbf24 amber to transparent) defined in src/index.css, triggered by the key prop remount mechanism on the VentanillaCard <p className="ventanilla-ticket"> element in src/App.tsx. The flash class is applied only when currentTicket is non-null.
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open http://localhost:5173 in a browser
    3. Press "Agregar ventanilla" to ensure at least one ventanilla is visible
    4. Press "Agregar turno" at least twice to add 2 tickets to the queue
    5. Press "Llamar siguiente" on the ventanilla — verify an amber flash (warm yellow-orange) appears on the "Turno N" text label and fades away within approximately 600ms
    6. Confirm that ONLY the ticket text paragraph flashes — the card background, "Ventanilla N" label, and "Llamar siguiente" button do NOT animate
    7. Press "Agregar turno" again to queue another ticket, then quickly press "Llamar siguiente" again — verify the amber flash restarts immediately from full amber (not stuck at a partial fade, not skipped)
    8. Reload the page (F5) — verify NO amber flash appears on any ventanilla on load (all start as 'sin turno', no flash class should be applied)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 visual checks pass (initial flash, scope limitation, rapid restart, no-load-flash). Or describe which specific visual behavior did not match.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reducer state → JSX render | ventanilla.currentTicket.id is a reducer-generated integer (never user-supplied string); used as React key |
| CSS class → browser paint | .ventanilla-ticket-flash is a static class name with no dynamic content; no injection vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | key prop value | accept | ventanilla.currentTicket?.id is sourced from queueReducer (pure function with typed action union, no user-input path); ?? 'empty' fallback produces valid React key types only |
| T-06-02-02 | Information Disclosure | CSS animation | accept | CSS class name and keyframe name are static strings in source; no PII or secret values animated |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan; zero supply chain exposure |
</threat_model>

<verification>
After Task 2 completes (before checkpoint):
  npm test — must exit 0 with 26 tests passing

After Task 3 (checkpoint approved):
  All ROADMAP Phase 6 success criteria are met:
  1. User sees transition animation when current ticket changes — verified by toHaveClass('ventanilla-ticket-flash') in 3 FEEDBACK-02 tests + visual checkpoint step 5
  2. Animation scoped to the ventanilla that changed, not the whole page — verified by visual checkpoint step 6 (only <p> flashes)
  3. No stuck or overlapping animation states on rapid calls — verified by key prop mechanism (each new key forces unmount+remount, resetting CSS state) + visual checkpoint step 7
</verification>

<success_criteria>
- src/index.css contains @keyframes ticket-flash (#fbbf24 → transparent, 600ms ease-out forwards) and .ventanilla-ticket-flash (animation + border-radius: 4px)
- src/App.tsx VentanillaCard <p> has key={ventanilla.currentTicket?.id ?? 'empty'} and conditional className including 'ventanilla-ticket-flash' only when currentTicket is non-null
- npm test exits 0 with all 26 tests passing (23 prior + 3 FEEDBACK-02)
- Visual checkpoint approved: amber flash appears on call, restarts on rapid calls, absent on page load
</success_criteria>

<output>
Create `.planning/phases/06-call-transition-animation/06-02-SUMMARY.md` when done.
</output>
