---
phase: 06-call-transition-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.test.tsx
autonomous: true
requirements:
  - FEEDBACK-02

must_haves:
  truths:
    - "FEEDBACK-02 describe block exists in App.test.tsx with 3 named test cases"
    - "npm test exits non-zero with exactly 3 FEEDBACK-02 test failures"
    - "All 23 previously-passing tests remain green after this task"
  artifacts:
    - path: src/App.test.tsx
      provides: "FEEDBACK-02 describe block appended after FEEDBACK-01 block"
      contains: "describe('FEEDBACK-02: Flash class on ticket change'"
  key_links:
    - from: "src/App.test.tsx FEEDBACK-02 describe block"
      to: "VentanillaCard named export in src/App.tsx"
      via: "import { VentanillaCard } from './App' (existing import on line 8)"
      pattern: "VentanillaCard"
---

## Phase Goal

**As a** clinic staff member, **I want to** see a brief visual animation on a ventanilla's ticket display when a new ticket is called, **so that** I can instantly spot which window just changed without scanning the entire screen.

Note: The ROADMAP Phase 6 `**Goal:**` line is not in canonical user-story format. This story was derived from the goal text. Run `/gsd mvp-phase 6` to set the canonical form if desired.

<objective>
Add a FEEDBACK-02 describe block with 3 failing test cases to src/App.test.tsx. These tests assert that the ventanilla-ticket-flash CSS class is applied conditionally to the `<p className="ventanilla-ticket">` element — a class that does not yet exist in the JSX. Tests must FAIL (RED) at the end of this plan. Wave 2 (Plan 06-02) implements the feature that makes them green.

Purpose: Establish testable behavior contracts before touching source code. The failing tests prove exactly what must change in App.tsx.
Output: src/App.test.tsx with FEEDBACK-02 describe block (3 it() cases) appended at end of file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/06-call-transition-animation/06-CONTEXT.md
@.planning/phases/06-call-transition-animation/06-PATTERNS.md
</context>

<interfaces>
From src/App.tsx (lines 6-16) — VentanillaCard props to use in tests:

  export function VentanillaCard({
    ventanilla,
    onRemove,
    onCallNext,
    queueLength,
  }: Readonly<{
    ventanilla: Ventanilla       // { id: number; number: number; currentTicket: Ticket | null }
    onRemove: (id: number) => void
    onCallNext: (id: number) => void
    queueLength: number
  }>)

Ticket type (inferred from App.test.tsx line 29):
  { id: number; number: number }

Existing App.test.tsx imports (lines 1-8 — do NOT duplicate these):
  const mockPlay = vi.fn()
  vi.mock('./useBeep', ...)
  import { render, screen, fireEvent, act } from '@testing-library/react'
  import { VentanillaCard } from './App'
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append FEEDBACK-02 describe block to App.test.tsx</name>
  <files>src/App.test.tsx</files>
  <read_first>
    src/App.test.tsx — read in full before editing; the new describe block appends after line 154 (the closing brace of the FEEDBACK-01 describe block). Note the existing import list (lines 1-8) — no new imports are needed.
    .planning/phases/06-call-transition-animation/06-PATTERNS.md — section "src/App.test.tsx — FEEDBACK-02 describe block" (lines 179-228) contains the verbatim describe block text including all 3 it() cases.
  </read_first>
  <behavior>
    - Test A: render VentanillaCard with currentTicket: { id: 5, number: 5 } and queueLength: 0 → screen.getByText('Turno 5') must have class 'ventanilla-ticket-flash'
    - Test B: render VentanillaCard with currentTicket: null and queueLength: 0 → screen.getByText('sin turno') must NOT have class 'ventanilla-ticket-flash'
    - Test C (rapid-call D-05): const { rerender } = render with currentTicket: { id: 10, number: 10 } → assert 'Turno 10' has flash class; rerender with currentTicket: { id: 11, number: 11 } → assert 'Turno 11' has flash class
    - All 3 tests use onRemove: () => {} and onCallNext: () => {} (no call-action side-effects needed)
    - No vi.useFakeTimers() or mockPlay.mockClear() needed — these tests check class presence via props only, not via button interaction
  </behavior>
  <action>
    Append the FEEDBACK-02 describe block at the end of src/App.test.tsx, after line 154 (the closing brace of the FEEDBACK-01 describe block). The block structure is:

    Outer describe: name is 'FEEDBACK-02: Flash class on ticket change' — follows the existing 'REQ-ID: Human-readable description' naming pattern used throughout the file.

    Test A — it name: 'applies ventanilla-ticket-flash class when currentTicket is non-null'. Render VentanillaCard with ventanilla prop { id: 1, number: 1, currentTicket: { id: 5, number: 5 } }, onRemove: () => {}, onCallNext: () => {}, queueLength: 0. Assert screen.getByText('Turno 5') toHaveClass 'ventanilla-ticket-flash'. The getByText call returns the `<p>` element directly because the text is a direct child of `<p>` — no .closest() needed.

    Test B — it name: 'does not apply ventanilla-ticket-flash class when currentTicket is null'. Render with currentTicket: null, queueLength: 0. Assert screen.getByText('sin turno') NOT toHaveClass 'ventanilla-ticket-flash'.

    Test C — it name: 'applies flash class on the new element after rapid ticket change (D-05)'. Use const { rerender } = render with ventanilla { id: 1, number: 1, currentTicket: { id: 10, number: 10 } }, queueLength: 0. Assert screen.getByText('Turno 10') toHaveClass 'ventanilla-ticket-flash'. Then rerender with the same structure but currentTicket: { id: 11, number: 11 }. Assert screen.getByText('Turno 11') toHaveClass 'ventanilla-ticket-flash'.

    Do NOT add any imports — VentanillaCard, render, screen, rerender are all already available from the existing import lines at the top of the file.
  </action>
  <verify>
    <automated>npm test</automated>
    Expected result: npm test exits NON-ZERO. The output shows 3 failing tests under "FEEDBACK-02: Flash class on ticket change". All other previously-passing tests (23 total) remain green. This RED state confirms the tests are real behavioral contracts, not vacuous.
  </verify>
  <acceptance_criteria>
    - src/App.test.tsx contains the string: describe('FEEDBACK-02: Flash class on ticket change'
    - src/App.test.tsx contains: toHaveClass('ventanilla-ticket-flash')
    - src/App.test.tsx contains: not.toHaveClass('ventanilla-ticket-flash')
    - src/App.test.tsx contains: rerender( (for Test C rapid-call case)
    - npm test output names exactly 3 failing tests, all under "FEEDBACK-02: Flash class on ticket change"
    - npm test output shows 0 failures outside the FEEDBACK-02 describe block
    - No new import lines added to App.test.tsx (all needed names already imported)
  </acceptance_criteria>
  <done>
    App.test.tsx has FEEDBACK-02 describe block with 3 failing tests. npm test exits non-zero. All 23 previously-passing tests remain green. Wave 1 is complete. Do NOT implement the feature until this RED state is verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test file → component | Test renders VentanillaCard with controlled props; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | App.test.tsx | accept | Test file is not shipped to production; no PII in test fixtures |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan; no supply chain exposure |
</threat_model>

<verification>
After this plan executes:
1. Run npm test — output must show exactly 3 failures, all in FEEDBACK-02 describe block
2. Confirm failure message contains "Expected element to have class: ventanilla-ticket-flash" or equivalent toHaveClass failure
3. Confirm no previously-green test now fails
4. Wave 1 is done — proceed to Plan 06-02 (Wave 2) only after confirming RED state
</verification>

<success_criteria>
- src/App.test.tsx contains FEEDBACK-02 describe block with 3 it() cases
- npm test exits non-zero with exactly 3 failures (all FEEDBACK-02)
- 23 prior tests remain green
- RED state confirmed: implementation has not yet been added
</success_criteria>

<output>
Create `.planning/phases/06-call-transition-animation/06-01-SUMMARY.md` when done.
</output>
