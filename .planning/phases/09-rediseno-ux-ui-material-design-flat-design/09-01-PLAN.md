---
phase: 09-rediseno-ux-ui-material-design-flat-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - index.html
autonomous: true
requirements:
  - D-23
  - D-18
must_haves:
  truths:
    - "npm test exits 0 after install — all 33 existing tests still pass"
    - "tailwindcss and @tailwindcss/vite appear in devDependencies in package.json"
    - "vite.config.ts imports tailwindcss and lists it in plugins alongside react()"
    - "index.html head contains the Google Fonts Roboto link with weights 400 and 700"
  artifacts:
    - path: "vite.config.ts"
      provides: "Tailwind v4 Vite plugin wired into build pipeline"
      contains: "import tailwindcss from '@tailwindcss/vite'"
    - path: "index.html"
      provides: "Roboto font loaded from Google Fonts CDN"
      contains: "fonts.googleapis.com/css2?family=Roboto"
  key_links:
    - from: "vite.config.ts plugins array"
      to: "@tailwindcss/vite plugin"
      via: "tailwindcss() call in plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "index.html head"
      to: "fonts.googleapis.com"
      via: "link rel=stylesheet"
      pattern: "family=Roboto"
---

<objective>
Install Tailwind CSS v4 as a devDependency and wire the Vite plugin so that subsequent plans can use `@import "tailwindcss"` and `@theme {}` in index.css. Add Google Fonts Roboto to index.html.

Purpose: All CSS work in Plans 09-02 and 09-03 depends on Tailwind being installed and the build pipeline recognizing `@import "tailwindcss"`. Without this wave, the `bg-md-primary` and other @theme-derived utility classes would be unknown. This plan is the only infrastructure prerequisite.

Output: Updated vite.config.ts (tailwindcss plugin added), updated index.html (Roboto link added), node_modules containing tailwindcss@^4 and @tailwindcss/vite@^4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-CONTEXT.md
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 + wire Vite plugin + add Roboto font</name>

  <read_first>
    - vite.config.ts — read current content before modifying (4 lines; preserve vitest reference directive and test block)
    - index.html — read current content before modifying (13 lines; insert Roboto link in head)
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md §Pattern 1 — exact vite.config.ts structure after Phase 9
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md §Pitfall 1 — breaking vitest config when adding Tailwind plugin
  </read_first>

  <files>vite.config.ts, index.html</files>

  <action>
    Step 1 — Install devDependencies. Run in project root:
    `npm install -D tailwindcss @tailwindcss/vite`
    Both packages are from the official tailwindlabs org on npm (slopcheck OK per RESEARCH.md §Package Legitimacy Audit). Expected versions: tailwindcss@4.3.2, @tailwindcss/vite@4.3.2. After install, verify both appear in node_modules.

    Step 2 — Update vite.config.ts. The file currently has four logical parts: the vitest reference directive on line 1, the vite/react imports, the defineConfig call with plugins: [react()], and the test block. Make exactly two changes:
    (a) Add `import tailwindcss from '@tailwindcss/vite'` as a new import line after `import react from '@vitejs/plugin-react'`.
    (b) Change `plugins: [react()]` to `plugins: [react(), tailwindcss()]`.
    Do NOT touch the `/// <reference types="vitest/config" />` directive, the `test:` block, `environment: 'jsdom'`, `setupFiles: './src/setupTests.ts'`, or `globals: true`. Per RESEARCH.md Pitfall 1 — removing those breaks `npm test`.

    Step 3 — Update index.html. In the `<head>` section, after the `<title>Turnero</title>` tag, insert:
    `<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">`
    Per D-18: load only weights 400 and 700. Do NOT load 300, 500, or 900. The `display=swap` parameter prevents FOIT (font invisible during load).

    Step 4 — Run npm test. All 33 tests must pass. This is the regression baseline before any CSS or JSX changes.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` exits 0 with 33 tests passing (no failures, no skipped)
    - `node_modules/tailwindcss/package.json` exists and contains `"version": "4.` (Tailwind v4 installed)
    - `node_modules/@tailwindcss/vite/package.json` exists and contains `"version": "4.` (@tailwindcss/vite v4 installed)
    - `vite.config.ts` contains the string `import tailwindcss from '@tailwindcss/vite'`
    - `vite.config.ts` contains the string `tailwindcss()` in the plugins array
    - `vite.config.ts` still contains `/// <reference types="vitest/config" />` on line 1
    - `vite.config.ts` still contains `environment: 'jsdom'`
    - `index.html` contains `fonts.googleapis.com/css2?family=Roboto:wght@400;700`
    - `index.html` contains `display=swap`
  </acceptance_criteria>

  <done>Tailwind v4 installed as devDependency, vite.config.ts wires the plugin without breaking vitest, index.html loads Roboto 400+700 from Google Fonts CDN, all 33 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → devDependency install | Package download during `npm install -D` |
| index.html → browser | Google Fonts CDN request at page load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | npm install (tailwindcss, @tailwindcss/vite) | accept | Both packages verified as official tailwindlabs packages; slopcheck [OK] per RESEARCH.md §Package Legitimacy Audit; no postinstall scripts |
| T-09-02 | Tampering | Google Fonts CDN stylesheet in index.html | accept | `<link>` loads CSS only (no JS executed); Google Fonts is a widely-trusted CDN; clinic display is in controlled environment (D-02) |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck confirmed [OK] for both packages; no [ASSUMED] or [SUS] packages in this plan |
</threat_model>

<verification>
After Task 1 completes:
- `npm test` → 33 passed, 0 failed
- `cat vite.config.ts | grep tailwindcss` → shows both the import line and the plugin call
- `cat index.html | grep Roboto` → shows the Google Fonts link with wght@400;700
</verification>

<success_criteria>
Tailwind v4 devDependencies installed, Vite plugin wired, Roboto font linked, vitest config intact, all 33 existing tests still green. Wave 2 plans (09-02, 09-03) can now execute.
</success_criteria>

<output>
Create `.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-01-SUMMARY.md` when done.
</output>
