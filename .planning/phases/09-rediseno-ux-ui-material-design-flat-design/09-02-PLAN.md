---
phase: 09-rediseno-ux-ui-material-design-flat-design
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/index.css
autonomous: true
requirements:
  - D-02
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-24
  - D-25
  - D-26
must_haves:
  truths:
    - "index.css starts with @import 'tailwindcss' as the first line"
    - "@theme block declares exactly five --color-* variables and one --font-sans variable"
    - "@keyframes ticket-flash FROM color is rgba(25, 118, 210, 0.25) — NOT amber #fbbf24"
    - ".ventanilla-ticket-flash class exists as plain CSS outside any @layer or @utility"
    - ".ventanillas-grid class declares grid-template-columns: repeat(auto-fill, minmax(350px, 1fr))"
    - "All 33 tests pass after this change"
  artifacts:
    - path: "src/index.css"
      provides: "Tailwind v4 entry point with MD3 color tokens and residual animation CSS"
      contains: "@import \"tailwindcss\""
    - path: "src/index.css"
      provides: "@theme block with MD3 color custom properties"
      contains: "--color-md-primary: #1976D2"
    - path: "src/index.css"
      provides: "Animation class preserved for FEEDBACK-02 tests"
      contains: ".ventanilla-ticket-flash"
  key_links:
    - from: "src/index.css @theme"
      to: "Tailwind utility classes"
      via: "@theme --color-* namespace generates bg-md-primary, text-md-primary, border-md-primary, text-md-error, etc."
      pattern: "--color-md-primary"
    - from: "src/index.css .ventanilla-ticket-flash"
      to: "App.test.tsx FEEDBACK-02 toHaveClass assertion"
      via: "DOM className attribute checked by toHaveClass('ventanilla-ticket-flash')"
      pattern: "\\.ventanilla-ticket-flash"
    - from: "src/index.css .ventanillas-grid"
      to: "App.tsx grid div className"
      via: "residual CSS class applied alongside Tailwind grid utilities"
      pattern: "auto-fill.*minmax"
---

<objective>
Replace src/index.css completely with the Tailwind v4 structure: @import directive, @theme block with MD3 color tokens, body reset, and two residual plain-CSS blocks (flash animation and ventanillas grid). All old custom classes (.page, .queue-strip, .ventanilla-card, etc.) are removed — their styles will come from Tailwind utilities applied in Plan 09-03.

Purpose: Establishes the CSS foundation that Plan 09-03 depends on. The @theme block generates the utility classes (bg-md-primary, text-md-error, etc.) that App.tsx will reference. The .ventanilla-ticket-flash residual class must survive this replacement for FEEDBACK-02 tests to pass.

Output: src/index.css replaced with ~50 lines implementing @import + @theme + body + flash animation + ventanillas-grid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-CONTEXT.md
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-UI-SPEC.md
@.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace src/index.css with Tailwind v4 structure and MD3 tokens</name>

  <read_first>
    - src/index.css — read current 182-line content before replacing; understand what must be removed vs kept
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md §Pattern 2 — exact full replacement structure
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-UI-SPEC.md §Component Contract (index.css section) — exact @keyframes and .ventanilla-ticket-flash values
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md §Pitfall 2 — @import must come before @theme
    - .planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-RESEARCH.md §Pitfall 3 — .ventanilla-ticket-flash must not be renamed
  </read_first>

  <files>src/index.css</files>

  <action>
    Replace the entire content of src/index.css. The new file has exactly this structure (order is critical per Pitfall 2):

    Section 1 — Tailwind import (must be the FIRST line, before anything else):
    `@import "tailwindcss";`

    Section 2 — MD3 color tokens and font. This @theme block must be top-level (not nested inside any rule). Declare exactly these six custom properties:
    - `--color-md-primary: #1976D2` (D-04: exact blue, not Tailwind blue-700 #1D4ED8)
    - `--color-md-primary-container: #BBDEFB` (D-06: primary container for card/strip backgrounds)
    - `--color-md-on-primary: #FFFFFF` (on-primary: text on primary blue surfaces)
    - `--color-md-on-primary-container: #1565C0` (on-primary-container: darker blue for contrast on #BBDEFB)
    - `--color-md-error: #B3261E` (D-07: MD3 error red, replaces old #c0392b)
    - `--font-sans: 'Roboto', sans-serif` (D-18: Roboto as default sans stack; generates font-sans utility)

    Section 3 — body reset:
    `body { margin: 0; background: #FFFFFF; font-family: 'Roboto', sans-serif; }`

    Section 4 — Flash animation residual block. Per D-26: keep as plain CSS outside any @layer or @utility so the class name is queryable by tests. Per D-08: update the FROM color from old amber #fbbf24 to primary blue with opacity.
    The @keyframes rule: name `ticket-flash`, `from { background-color: rgba(25, 118, 210, 0.25); }`, `to { background-color: transparent; }`.
    The .ventanilla-ticket-flash rule: `animation: ticket-flash 600ms ease-out forwards;` and `border-radius: 8px;` (increased from old 4px per UI-SPEC — better proportions with 48px ticket display).
    Do NOT put this inside @layer, @utility, or any other wrapper. Plain CSS at the top level.

    Section 5 — Ventanillas grid residual block. Per RESEARCH.md Pitfall 5 and UI-SPEC recommended approach 1: keep as a plain CSS class rather than an arbitrary Tailwind value.
    The .ventanillas-grid rule: `grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));`
    This preserves the Phase 7 DISPLAY-01 constraint. The display:grid and gap will be handled by Tailwind utilities in App.tsx (grid gap-4).

    Remove everything else from the old index.css: :root, .page, .page-title, .queue-strip, .add-ticket-button, .ticket-list, .ticket-chip, .ventanilla-card, .ventanilla-label, .ventanilla-ticket, .ventanilla-remove, .ventanilla-remove:hover, .ventanilla-warning, .call-next-button, .ventanillas-empty, .ventanillas-section, .add-window-button, the old .ventanillas-grid > * rule. All these styles will come from Tailwind utilities in Plan 09-03.

    After writing the file, run npm test to confirm all 33 tests still pass. The CSS change alone should not break any test since tests check DOM class names and behavior, not computed CSS styles.
  </action>

  <verify>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npm test` exits 0 with 33 tests passing
    - `src/index.css` first line is exactly `@import "tailwindcss";` (no whitespace or comment before it)
    - `src/index.css` contains `--color-md-primary: #1976D2` inside an `@theme` block
    - `src/index.css` contains `--color-md-primary-container: #BBDEFB` inside an `@theme` block
    - `src/index.css` contains `--color-md-on-primary-container: #1565C0` inside an `@theme` block
    - `src/index.css` contains `--color-md-error: #B3261E` inside an `@theme` block
    - `src/index.css` contains `--font-sans: 'Roboto', sans-serif` inside an `@theme` block
    - `src/index.css` contains `rgba(25, 118, 210, 0.25)` (NOT `#fbbf24`) in the @keyframes ticket-flash from block
    - `src/index.css` contains `.ventanilla-ticket-flash` as a plain CSS rule (not wrapped in @layer or @utility)
    - `src/index.css` contains `animation: ticket-flash 600ms ease-out forwards` inside `.ventanilla-ticket-flash`
    - `src/index.css` contains `border-radius: 8px` inside `.ventanilla-ticket-flash`
    - `src/index.css` contains `grid-template-columns: repeat(auto-fill, minmax(350px, 1fr))` inside `.ventanillas-grid`
    - `src/index.css` does NOT contain `#fbbf24` (old amber flash color must be gone)
    - `src/index.css` does NOT contain `.page-title` (old class must be removed)
    - `src/index.css` does NOT contain `.queue-strip` (old class must be removed)
    - `src/index.css` does NOT contain `.ventanilla-card` (old class must be removed)
    - `src/index.css` does NOT contain `@media (prefers-color-scheme: dark)` (D-02: light mode only)
  </acceptance_criteria>

  <done>src/index.css contains only: @import "tailwindcss", @theme MD3 tokens, body reset, ticket-flash animation (updated color), .ventanilla-ticket-flash plain CSS, .ventanillas-grid template columns. All 33 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.css → Tailwind CSS pipeline | Tailwind v4 processes @import and @theme at build time; no runtime JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | CSS cascade — plain CSS vs Tailwind utilities | accept | Per RESEARCH.md Pattern 5: unlayered plain CSS (like .ventanilla-ticket-flash) has higher cascade priority than Tailwind's layered utilities. This is intentional — animation styles cannot be accidentally overridden by a utility class |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan; tailwindcss already installed in Wave 1 |
</threat_model>

<verification>
After Task 1 completes:
- `npm test` → 33 passed, 0 failed
- `grep -c "fbbf24" src/index.css` → 0 (old amber color gone)
- `grep "rgba(25, 118, 210" src/index.css` → shows the from block with the new blue flash color
- `grep "ventanilla-ticket-flash" src/index.css` → shows the .ventanilla-ticket-flash rule
- `grep "auto-fill" src/index.css` → shows the grid-template-columns declaration
</verification>

<success_criteria>
src/index.css replaced with Tailwind v4 structure. @theme declares all five MD3 color tokens plus font-sans. @keyframes ticket-flash uses rgba(25, 118, 210, 0.25) as FROM color. .ventanilla-ticket-flash remains as plain CSS. .ventanillas-grid preserves Phase 7 grid layout. All 33 tests green.
</success_criteria>

<output>
Create `.planning/phases/09-rediseno-ux-ui-material-design-flat-design/09-02-SUMMARY.md` when done.
</output>
