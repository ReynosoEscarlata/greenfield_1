---
phase: 05-call-sound-feedback
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/useBeep.ts
  - src/setupTests.ts
  - src/App.test.tsx
autonomous: true
requirements:
  - FEEDBACK-01

must_haves:
  truths:
    - "src/useBeep.ts exists and exports a named useBeep function (even if stub)"
    - "src/setupTests.ts defines global.AudioContext as a vi.fn() mock"
    - "App.test.tsx has vi.mock('./useBeep') hoisted at the top"
    - "All existing VentanillaCard renders in App.test.tsx use queueLength instead of isQueueEmpty"
    - "App.test.tsx has a FEEDBACK-01 describe block with two failing tests (play called / play not called)"
    - "Running npm test exits non-zero (RED — tests fail because component not yet updated)"
  artifacts:
    - path: "src/useBeep.ts"
      provides: "Empty stub so vi.mock can resolve the module path"
      exports: ["useBeep"]
    - path: "src/setupTests.ts"
      provides: "Global AudioContext mock for all test files"
      contains: "global.AudioContext = vi.fn"
    - path: "src/App.test.tsx"
      provides: "Failing FEEDBACK-01 tests + prop-rename updates"
      contains: "vi.mock('./useBeep'"
  key_links:
    - from: "src/App.test.tsx (vi.mock)"
      to: "src/useBeep.ts"
      via: "Vitest module mock factory"
      pattern: "vi.mock\\('./useBeep'"
    - from: "src/App.test.tsx (FEEDBACK-01 tests)"
      to: "mockPlay"
      via: "module-level vi.fn() captured before vi.mock call"
      pattern: "mockPlay\\.toHaveBeenCalledOnce"
---

## Phase Goal

**As a** sala de espera operator, **I want to** hear a beep when I successfully call a ticket, **so that** I receive immediate audible confirmation that the queue advanced.

<objective>
Write the RED (failing) tests for FEEDBACK-01 and set up the test infrastructure that the GREEN phase depends on.

Purpose: Establish the contract — what must be true after implementation — before writing any production code. Running tests after this plan exits non-zero, proving the RED state.

Output:
- `src/useBeep.ts` — minimal stub (exports `useBeep` returning `{ play }` as no-op) so module resolution works with `vi.mock`
- `src/setupTests.ts` — extended with global `AudioContext` mock covering the methods `useBeep.play()` exercises
- `src/App.test.tsx` — `vi.mock('./useBeep')` hoisted at top, all 7 `isQueueEmpty` prop references replaced with `queueLength`, new `describe('FEEDBACK-01: ...')` block with 2 tests
</objective>

<execution_context>
@C:/Users/Richi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Richi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-call-sound-feedback/05-CONTEXT.md
@.planning/phases/05-call-sound-feedback/05-RESEARCH.md
@.planning/phases/05-call-sound-feedback/05-PATTERNS.md

<interfaces>
<!-- Current state of files being modified — executor MUST read before touching anything -->

From src/App.tsx (lines 5–14, current VentanillaCard prop signature):
  isQueueEmpty: boolean   ← this prop name is being REPLACED by queueLength: number in Plan 05-02
  (do NOT change App.tsx in this plan — tests go RED precisely because the component still uses isQueueEmpty)

From src/App.test.tsx (lines with isQueueEmpty that must become queueLength):
  Line 11:  isQueueEmpty={false}  → queueLength={1}
  Line 24:  isQueueEmpty={false}  → queueLength={1}
  Line 38:  isQueueEmpty={false}  → queueLength={1}
  Line 56:  isQueueEmpty={false}  → queueLength={1}
  Line 79:  isQueueEmpty={true}   → queueLength={0}
  Line 99:  isQueueEmpty={false}  → queueLength={1}
  Line 109: isQueueEmpty={true}   → queueLength={0}

From src/setupTests.ts (current — single line):
  import '@testing-library/jest-dom'
  (AudioContext mock is appended AFTER this import)

Key: vi is available globally in setupTests.ts without import
     (vite.config.ts sets globals: true in test block)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Create useBeep stub + extend setupTests.ts with AudioContext mock</name>
  <files>src/useBeep.ts, src/setupTests.ts</files>

  <read_first>
    - src/setupTests.ts — read the current single line; append mock AFTER the existing import
    - src/turnero.ts — skim the named export pattern (export function name() { ... }); useBeep.ts follows the same skeleton
  </read_first>

  <behavior>
    - useBeep.ts: exports named function useBeep() that returns { play } where play is a no-op () => void — this stub is enough for vi.mock to resolve the module path
    - setupTests.ts: after the existing import line, defines createMockOscillator (factory returning object with type, frequency.setValueAtTime, connect, start, stop as vi.fn()), createMockGain (factory returning object with gain.setValueAtTime, gain.exponentialRampToValueAtTime, connect as vi.fn()), mockAudioContextInstance (object with createOscillator: vi.fn(createMockOscillator), createGain: vi.fn(createMockGain), destination: {}, currentTime: 0, state: 'running', resume: vi.fn()), then assigns global.AudioContext = vi.fn(() => mockAudioContextInstance) as unknown as typeof AudioContext
  </behavior>

  <action>
    Create src/useBeep.ts as a minimal named-export stub. The file contains a module-level let audioCtx variable typed AudioContext | null = null, then exports function useBeep() that defines an inner function play() which is an empty body (no-op), and returns { play }. This stub is intentionally empty — its only purpose is to be resolvable by vi.mock in App.test.tsx; the real implementation comes in Plan 05-02.

    Extend src/setupTests.ts by appending the AudioContext mock AFTER the existing import '@testing-library/jest-dom' line. The mock structure must cover every method that useBeep.play() calls:
    - createMockOscillator factory: returns object with type property ('sine' as OscillatorType), frequency.setValueAtTime as vi.fn(), connect as vi.fn(), start as vi.fn(), stop as vi.fn()
    - createMockGain factory: returns object with gain.setValueAtTime as vi.fn(), gain.exponentialRampToValueAtTime as vi.fn(), connect as vi.fn()
    - mockAudioContextInstance: object with createOscillator: vi.fn(createMockOscillator), createGain: vi.fn(createMockGain), destination: {}, currentTime: 0, state: 'running', resume: vi.fn()
    - global.AudioContext = vi.fn(() => mockAudioContextInstance) as unknown as typeof AudioContext

    Do NOT add vi import — vi is already global (vite.config.ts globals: true).
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/useBeep.ts exists and contains export function useBeep
    - src/useBeep.ts contains let audioCtx: AudioContext | null = null at module level
    - src/useBeep.ts returns { play } from useBeep()
    - src/setupTests.ts contains global.AudioContext = vi.fn
    - src/setupTests.ts contains createMockOscillator
    - src/setupTests.ts contains exponentialRampToValueAtTime: vi.fn()
    - src/setupTests.ts does NOT add an import for vi (vi is already global)
  </acceptance_criteria>

  <done>useBeep stub exists; setupTests.ts has full AudioContext mock; tsc --noEmit passes on these two files in isolation</done>
</task>

<task type="tdd">
  <name>Task 2 (RED): Update App.test.tsx — add vi.mock, rename props, add FEEDBACK-01 describe block</name>
  <files>src/App.test.tsx</files>

  <read_first>
    - src/App.test.tsx — read the full file first; identify every isQueueEmpty occurrence and its line context before editing
    - src/App.tsx — confirm VentanillaCard still has isQueueEmpty: boolean (confirming RED state: tests will fail because component not yet updated)
    - .planning/phases/05-call-sound-feedback/05-PATTERNS.md — PATTERNS.md lines 249–316 show the exact new describe block content and vi.mock placement
  </read_first>

  <behavior>
    - vi.mock('./useBeep') hoisted call appears BEFORE existing imports (Vitest hoists vi.mock automatically, but declare it early for clarity)
    - mockPlay = vi.fn() declared before the vi.mock call so it is in scope inside the factory
    - All 7 existing VentanillaCard renders replace isQueueEmpty prop: isQueueEmpty={false} becomes queueLength={1}, isQueueEmpty={true} becomes queueLength={0}
    - New describe block 'FEEDBACK-01: Beep on successful call' added after the WR-01 block
    - Inside FEEDBACK-01 describe: beforeEach resets mockPlay via mockPlay.mockClear(); first test renders with queueLength={1}, clicks "Llamar siguiente", asserts mockPlay.toHaveBeenCalledOnce(); second test renders with queueLength={0}, clicks "Llamar siguiente", asserts mockPlay.not.toHaveBeenCalled()
    - After all changes: npm test exits non-zero — RED state because VentanillaCard still expects isQueueEmpty: boolean but tests now pass queueLength: number
  </behavior>

  <action>
    Prepend to src/App.test.tsx (before the existing import lines):
      const mockPlay = vi.fn()
      vi.mock('./useBeep', () => ({ useBeep: () => ({ play: mockPlay }) }))

    Then update ALL 7 occurrences of isQueueEmpty in the file's render calls. Use the exact line mapping from PATTERNS.md — every isQueueEmpty={false} becomes queueLength={1} and every isQueueEmpty={true} becomes queueLength={0}. Do not change any other test logic.

    Then append a new describe block after the final existing describe block (WR-01). The block is named 'FEEDBACK-01: Beep on successful call' and contains:
    - beforeEach that calls mockPlay.mockClear()
    - it('calls play() when queue has tickets') — renders VentanillaCard with queueLength={1}, fires click on "Llamar siguiente" button, expects mockPlay.toHaveBeenCalledOnce()
    - it('does not call play() when queue is empty') — renders VentanillaCard with queueLength={0}, fires click on "Llamar siguiente" button, expects mockPlay.not.toHaveBeenCalled()

    VentanillaCard props in both new tests: ventanilla={{ id: 1, number: 1, currentTicket: null }}, onRemove={() => {}}, onCallNext={() => {}}, plus the respective queueLength value.
  </action>

  <verify>
    <automated>npm test 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - src/App.test.tsx line 1 contains const mockPlay = vi.fn()
    - src/App.test.tsx contains vi.mock('./useBeep'
    - src/App.test.tsx contains no remaining occurrences of isQueueEmpty (grep -c "isQueueEmpty" src/App.test.tsx returns 0)
    - src/App.test.tsx contains queueLength={1} and queueLength={0}
    - src/App.test.tsx contains describe('FEEDBACK-01:
    - src/App.test.tsx contains mockPlay.toHaveBeenCalledOnce()
    - src/App.test.tsx contains mockPlay.not.toHaveBeenCalled()
    - npm test exits non-zero (RED — TypeScript error because VentanillaCard still has isQueueEmpty prop)
    - commit message format: test(05-01): add failing FEEDBACK-01 tests — RED
  </acceptance_criteria>

  <done>App.test.tsx fully updated with vi.mock, all prop renames applied, FEEDBACK-01 describe block present; npm test fails (RED confirmed); RED commit made</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production module | vi.mock intercepts useBeep import; no trust boundary violation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/setupTests.ts global mock | accept | Mock only affects test environment (jsdom); cannot affect production AudioContext behavior |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — Web Audio API is native; threat does not apply |
</threat_model>

<verification>
After both tasks complete:
1. src/useBeep.ts exists with named export useBeep
2. src/setupTests.ts contains global.AudioContext = vi.fn(...)
3. src/App.test.tsx has no isQueueEmpty occurrences
4. src/App.test.tsx has vi.mock('./useBeep', ...) at top
5. src/App.test.tsx has FEEDBACK-01 describe block
6. npm test exits non-zero (RED state confirmed)
</verification>

<success_criteria>
RED phase complete: test infrastructure exists, tests are written against the new API (queueLength prop + mockPlay), and npm test fails because the component has not yet been updated. A RED commit is made before moving to Plan 05-02.
</success_criteria>

<output>
Create `.planning/phases/05-call-sound-feedback/05-01-SUMMARY.md` when done.
</output>
