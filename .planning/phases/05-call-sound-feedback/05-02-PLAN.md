---
phase: 05-call-sound-feedback
plan: 02
type: tdd
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/useBeep.ts
  - src/App.tsx
autonomous: true
requirements:
  - FEEDBACK-01

must_haves:
  truths:
    - "User hears a beep immediately when pressing Llamar siguiente and a ticket is taken"
    - "No beep plays when Llamar siguiente is pressed on an empty queue"
    - "No beep plays on page load or reload (beep is not in any useEffect)"
    - "npm test exits 0 — all tests including FEEDBACK-01 pass (GREEN)"
  artifacts:
    - path: "src/useBeep.ts"
      provides: "Lazy singleton AudioContext + per-call OscillatorNode beep"
      exports: ["useBeep"]
      contains: "exponentialRampToValueAtTime(0.001"
    - path: "src/App.tsx"
      provides: "VentanillaCard with queueLength prop + playBeep() in handleCallNext"
      contains: "import { useBeep } from './useBeep'"
  key_links:
    - from: "src/App.tsx (VentanillaCard handleCallNext)"
      to: "useBeep.play()"
      via: "const { play: playBeep } = useBeep() inside component body"
      pattern: "playBeep\\(\\)"
    - from: "src/App.tsx (App component JSX)"
      to: "VentanillaCard"
      via: "queueLength={state.queue.length} prop"
      pattern: "queueLength=\\{state\\.queue\\.length\\}"
---

<objective>
Implement the GREEN phase: replace the useBeep stub with the full lazy-singleton AudioContext beep, and update VentanillaCard in App.tsx to consume the new queueLength prop and call playBeep() synchronously inside the click handler.

Purpose: Make all tests pass. The FEEDBACK-01 describe block written in Plan 05-01 must exit green. The component change (isQueueEmpty → queueLength) resolves the TypeScript errors from the RED commit.

Output:
- `src/useBeep.ts` — full implementation: lazy singleton AudioContext, per-call OscillatorNode+GainNode at 880 Hz/200ms/decay-to-0.001, silent try/catch
- `src/App.tsx` — VentanillaCard updated: import useBeep, prop renamed isQueueEmpty → queueLength (number), handleCallNext calls playBeep() before onCallNext(), App JSX passes queueLength={state.queue.length}
</objective>

<execution_context>
@C:/Users/Richi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Richi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-call-sound-feedback/05-CONTEXT.md
@.planning/phases/05-call-sound-feedback/05-RESEARCH.md
@.planning/phases/05-call-sound-feedback/05-PATTERNS.md
@.planning/phases/05-call-sound-feedback/05-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs — extracted from prior plan and codebase -->

From src/useBeep.ts (stub from Plan 05-01 — replace body fully):
  let audioCtx: AudioContext | null = null
  export function useBeep() {
    function play() { /* currently empty — replace with full implementation */ }
    return { play }
  }

From src/App.tsx (current VentanillaCard props — these change in this plan):
  isQueueEmpty: boolean   ← rename to queueLength: number
  Readonly<{...}> wrapper ← keep (SonarLint S6759 project convention)

From src/App.tsx (current handleCallNext — lines 42–48):
  function handleCallNext() {
    if (isQueueEmpty) {       ← becomes: if (queueLength === 0)
      setShowEmptyWarning(true)
      return
    }
    onCallNext(ventanilla.id) ← add playBeep() BEFORE this line
  }

From src/App.tsx (App component JSX — line 129):
  isQueueEmpty={state.queue.length === 0}  ← becomes: queueLength={state.queue.length}

beep spec (D-01/D-02):
  frequency: 880 Hz, type: 'sine'
  gain initial: 0.3
  gain decay target: 0.001 (NOT 0 — exponential ramp undefined at zero)
  duration: 0.2 seconds (ctx.currentTime + 0.2)
  AudioContext: lazy singleton (module-level let audioCtx: AudioContext | null = null)
  OscillatorNode: new instance per call (one-shot — cannot restart after stop)
  Error handling: silent try/catch (D-07)
  playBeep() call order: BEFORE onCallNext() to stay inside synchronous user gesture (D-06)
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (GREEN): Implement useBeep.ts — lazy singleton beep with decay envelope</name>
  <files>src/useBeep.ts</files>

  <read_first>
    - src/useBeep.ts — read the current stub; replace the empty play() body only; keep the module-level audioCtx variable and the return { play } structure
    - .planning/phases/05-call-sound-feedback/05-RESEARCH.md — lines 343–384 contain the complete verified implementation to use as reference (Pattern 1: Lazy Singleton AudioContext)
    - .planning/phases/05-call-sound-feedback/05-PATTERNS.md — lines 66–99 reproduce the same implementation with inline comments
  </read_first>

  <behavior>
    - play() checks if audioCtx is null and creates new AudioContext() if so (lazy singleton — created inside user gesture, starts in running state automatically)
    - play() creates a new OscillatorNode per call via ctx.createOscillator() — OscillatorNode is one-shot, cannot restart after stop()
    - play() creates a new GainNode per call via ctx.createGain()
    - osc.type set to 'sine', osc.frequency.setValueAtTime(880, now) for 880 Hz (D-02)
    - gain.gain.setValueAtTime(0.3, now) for initial volume (D-02)
    - gain.gain.exponentialRampToValueAtTime(0.001, now + 0.2) — target is 0.001 NOT 0 (RESEARCH Pitfall 3)
    - audio graph: osc.connect(gain), gain.connect(ctx.destination)
    - osc.start(now), osc.stop(now + 0.2) — 200ms duration (D-02)
    - entire play() body wrapped in try/catch with empty catch block (D-07 silent failure)
  </behavior>

  <action>
    Replace the empty body of play() in src/useBeep.ts with the full lazy-singleton implementation.

    The module-level variable let audioCtx: AudioContext | null = null stays at the top of the file, outside the hook.

    Inside play():
    1. try { block
    2. Lazy init: if (!audioCtx) { audioCtx = new AudioContext() }
    3. Capture: const ctx = audioCtx; const now = ctx.currentTime
    4. Create nodes: const osc = ctx.createOscillator(); const gain = ctx.createGain()
    5. Configure oscillator: osc.type = 'sine'; osc.frequency.setValueAtTime(880, now)
    6. Configure gain envelope: gain.gain.setValueAtTime(0.3, now); gain.gain.exponentialRampToValueAtTime(0.001, now + 0.2)
    7. Connect graph: osc.connect(gain); gain.connect(ctx.destination)
    8. Fire: osc.start(now); osc.stop(now + 0.2)
    9. } catch { // silent — audio failure must not interrupt call-next flow }

    Do NOT add useCallback wrapper around play (RESEARCH.md A1 — play reads module-level state, not React props; memoization provides no benefit).
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose 2>&1 | grep -E "(FEEDBACK|PASS|FAIL|useBeep)"</automated>
  </verify>

  <acceptance_criteria>
    - src/useBeep.ts contains exponentialRampToValueAtTime(0.001
    - src/useBeep.ts does NOT contain exponentialRampToValueAtTime(0, (target must not be zero)
    - src/useBeep.ts contains osc.start(now)
    - src/useBeep.ts contains osc.stop(now + 0.2)
    - src/useBeep.ts contains try { and } catch {
    - src/useBeep.ts contains if (!audioCtx) (lazy singleton guard)
    - src/useBeep.ts does NOT contain useCallback (no memoization needed)
    - Running only the useBeep-related paths: the mock in setupTests.ts means AudioContext constructor calls are intercepted; no real audio thread is started during tests
  </acceptance_criteria>

  <done>useBeep.ts fully implemented; all Web Audio API calls present; silent try/catch in place</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Update App.tsx — rename isQueueEmpty prop, import useBeep, add playBeep() to click handler</name>
  <files>src/App.tsx</files>

  <read_first>
    - src/App.tsx — read the full file; identify VentanillaCard function signature (lines 5–15), handleCallNext (lines 42–48), and JSX prop pass-through (lines 124–130); apply 4 surgical changes
    - src/App.test.tsx — skim to confirm queueLength={1}/queueLength={0} is now used everywhere; this is the contract the component must match
    - .planning/phases/05-call-sound-feedback/05-PATTERNS.md — lines 104–195 show the exact before/after diff for all 4 changes
  </read_first>

  <behavior>
    - import { useBeep } from './useBeep' added as third import line (after turnero imports)
    - VentanillaCard Readonly<{...}> props type: isQueueEmpty: boolean replaced with queueLength: number
    - VentanillaCard destructured parameter: isQueueEmpty replaced with queueLength
    - Inside VentanillaCard body (after existing useState calls): const { play: playBeep } = useBeep()
    - handleCallNext: if (isQueueEmpty) becomes if (queueLength === 0); before onCallNext(ventanilla.id) add playBeep() call (per D-06: playBeep must be called before onCallNext to remain in synchronous user-gesture context)
    - App component JSX: isQueueEmpty={state.queue.length === 0} replaced with queueLength={state.queue.length}
    - No other changes to App.tsx (component tree, CSS classes, warning JSX — untouched)
  </behavior>

  <action>
    Make exactly 4 targeted changes to src/App.tsx:

    Change 1 — Add import: After line 3 (import type { Ventanilla } from './turnero'), add:
      import { useBeep } from './useBeep'

    Change 2 — Rename prop in VentanillaCard signature: In the destructured parameter list and the Readonly<{...}> type, replace isQueueEmpty with queueLength and change its type from boolean to number.

    Change 3 — Update VentanillaCard body: Add const { play: playBeep } = useBeep() after the existing useState declarations (setShowWarning, setShowEmptyWarning). Then in handleCallNext: replace if (isQueueEmpty) with if (queueLength === 0), and add playBeep() on a new line immediately before the existing onCallNext(ventanilla.id) call — the order is playBeep() then onCallNext(), keeping the beep inside the synchronous user gesture per D-06.

    Change 4 — Update JSX prop: In the App component's map() call, replace isQueueEmpty={state.queue.length === 0} with queueLength={state.queue.length}.

    Do NOT touch: useEffect blocks, JSX warning paragraphs, CSS class names, the handleRemove function, the queue section, or the ventanilla section layout.
  </action>

  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - src/App.tsx contains import { useBeep } from './useBeep'
    - src/App.tsx contains queueLength: number in the VentanillaCard Readonly<{...}> type
    - src/App.tsx does NOT contain isQueueEmpty anywhere (grep -c "isQueueEmpty" src/App.tsx returns 0)
    - src/App.tsx contains const { play: playBeep } = useBeep()
    - src/App.tsx contains playBeep() on a line immediately before onCallNext(ventanilla.id) inside handleCallNext
    - src/App.tsx contains queueLength={state.queue.length} in the App JSX (not queueLength === 0)
    - npm test exits 0 — all test suites pass including FEEDBACK-01 describe block
    - npx tsc --noEmit exits 0 — no TypeScript errors
    - commit message format: feat(05-02): implement useBeep and wire into VentanillaCard — GREEN
  </acceptance_criteria>

  <done>App.tsx updated; npm test exits 0; tsc --noEmit exits 0; GREEN commit made</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → AudioContext | AudioContext created inside synchronous click handler — satisfies browser autoplay policy |
| AudioContext → audio hardware | Entirely managed by browser; no trust boundary the app controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Denial of Service | AudioContext creation per-call | accept | AudioContext is a singleton (created once); OscillatorNode is lightweight and short-lived (200ms); DoS via rapid clicking would require hardware rate limits, not a software concern for a local clinic kiosk |
| T-05-03 | Information Disclosure | Audio output | accept | Synthesized tone at fixed frequency; reveals nothing about patient data or system state |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — Web Audio API is native; supply chain threat does not apply |
</threat_model>

<verification>
After both plans (05-01 + 05-02) complete:
1. npm test exits 0
2. npx tsc --noEmit exits 0
3. src/App.tsx grep "isQueueEmpty" returns 0 matches
4. src/App.test.tsx grep "isQueueEmpty" returns 0 matches
5. src/useBeep.ts exports useBeep function
6. src/useBeep.ts contains exponentialRampToValueAtTime(0.001
7. src/App.tsx contains playBeep() before onCallNext in handleCallNext
8. Manual check (checkpoint:human-verify not needed here — behavior is auditory and tested via mock): in browser dev tools, clicking "Llamar siguiente" with tickets in queue logs no AudioContext errors in console
</verification>

<success_criteria>
GREEN phase complete: npm test exits 0 with all tests passing (including FEEDBACK-01), tsc --noEmit exits 0, and the beep plays synchronously inside the click handler with no useEffect involvement. A GREEN commit is made.
</success_criteria>

<output>
Create `.planning/phases/05-call-sound-feedback/05-02-SUMMARY.md` when done.
</output>
