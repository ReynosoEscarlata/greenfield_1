---
phase: 04-call-next-atomic-dequeue
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/App.tsx
  - src/index.css
autonomous: false
requirements:
  - CALL-01
  - CALL-02

must_haves:
  truths:
    - "Every VentanillaCard renders a 'Llamar siguiente' button below the current-ticket display, full-width, at all times (D-01, D-07)"
    - "Clicking 'Llamar siguiente' on a non-empty queue calls onCallNext(ventanilla.id) — dispatch wires to CALL_NEXT reducer action"
    - "Clicking 'Llamar siguiente' on an empty queue shows 'No hay turnos en espera' inline inside the card that was clicked"
    - "The empty-queue message auto-dismisses after 2 seconds via useEffect cleanup setTimeout pattern"
    - "showWarning (removal guard) resets to false when ventanilla.currentTicket changes to null externally (WR-01)"
    - "All 21 tests pass (15 unit + 6 integration)"
  artifacts:
    - path: "src/App.tsx"
      provides: "VentanillaCard with onCallNext + isQueueEmpty props, WR-01 useEffect, CALL-02 useEffect, handleCallNext function, Llamar siguiente button + empty-queue warning JSX; App dispatch wiring"
      contains: "onCallNext: (id: number) => void"
    - path: "src/index.css"
      provides: ".call-next-button CSS rule (full-width, follows .add-window-button conventions)"
      contains: ".call-next-button"
  key_links:
    - from: "src/App.tsx VentanillaCard handleCallNext"
      to: "src/App.tsx App dispatch"
      via: "onCallNext prop callback → dispatch({ type: 'CALL_NEXT', windowId: id })"
      pattern: "CALL_NEXT.*windowId"
    - from: "src/App.tsx VentanillaCard"
      to: "src/App.tsx App state"
      via: "isQueueEmpty={state.queue.length === 0} prop"
      pattern: "isQueueEmpty.*queue.length"
    - from: "src/App.tsx WR-01 useEffect"
      to: "ventanilla.currentTicket prop"
      via: "dep array [ventanilla.currentTicket] — fires when prop changes to null"
      pattern: "ventanilla\\.currentTicket.*null"
---

<objective>
Extend VentanillaCard with the "Llamar siguiente" button and empty-queue warning, wire the new dispatch in App.tsx, and add the .call-next-button CSS rule. This plan turns all 6 integration tests from RED to GREEN, completing the Core Value end-to-end.

Purpose: Plan 01 gave us a tested reducer. This plan adds the visible user-facing layer that fulfills CALL-01 (the button) and CALL-02 (empty-queue feedback), making Phase 4's observable goal reachable from the browser.
Output: Extended src/App.tsx (VentanillaCard with new props/hooks/JSX + App render wiring), extended src/index.css (.call-next-button rule), all 21 tests GREEN, visual checkpoint passed.
</objective>

<execution_context>
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-CONTEXT.md
</execution_context>

<context>
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/PROJECT.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/ROADMAP.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-PATTERNS.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-01-SUMMARY.md

<interfaces>
<!-- Key contracts extracted from src/App.tsx (current state — READ the file before modifying). -->

Current VentanillaCard signature (lines 5-11):
  export function VentanillaCard({
    ventanilla,
    onRemove,
  }: Readonly<{
    ventanilla: Ventanilla
    onRemove: (id: number) => void
  }>) {

Current import line (line 1):
  import { useState, useReducer } from 'react'
  // Must add: useEffect

Current local state (line 12):
  const [showWarning, setShowWarning] = useState(false)
  // Must add: const [showEmptyWarning, setShowEmptyWarning] = useState(false)

Current JSX structure in VentanillaCard (lines 23-46):
  <div className="ventanilla-card">
    <button ... className="ventanilla-remove" ...>×</button>
    <h3 className="ventanilla-label">Ventanilla {ventanilla.number}</h3>
    <p className="ventanilla-ticket">...</p>
    {showWarning && (<p className="ventanilla-warning">...</p>)}
  </div>
  // After plan: add <button className="call-next-button"> BELOW <p.ventanilla-ticket> and ABOVE showWarning

Current VentanillaCard invocation in App JSX (lines 91-94):
  <VentanillaCard
    key={v.id}
    ventanilla={v}
    onRemove={(id) => dispatch({ type: 'REMOVE_WINDOW', id })}
  />
  // Must add: onCallNext={(id) => dispatch({ type: 'CALL_NEXT', windowId: id })}
  //           isQueueEmpty={state.queue.length === 0}

<!-- Key contracts extracted from src/index.css (reference button). -->

.add-window-button (lines 94-104) — copy as template for .call-next-button:
  background: #f1f3f5; color: #1f2933; border: none; border-radius: 6px;
  padding: 12px 16px; font-size: 16px; font-weight: 600; cursor: pointer; margin: 0 0 16px;
  // .call-next-button adds: width: 100%; margin: 8px 0 0;
  // Append after .ventanilla-warning rule (line 144), before .ventanillas-empty
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend VentanillaCard in App.tsx + add CSS — TDD Green gate for integration tests</name>
  <files>src/App.tsx, src/index.css</files>
  <read_first>
    - src/App.tsx — read entire file before editing; understand the current VentanillaCard structure end-to-end (lines 1-46) and the App JSX VentanillaCard invocation (lines 91-94)
    - src/index.css — read entire file; identify insertion point after .ventanilla-warning rule (line 141-144) and before .ventanillas-empty (line 147)
    - .planning/phases/04-call-next-atomic-dequeue/04-PATTERNS.md — sections "src/App.tsx" and "src/index.css" contain the exact code to implement; do NOT deviate from the patterns shown
    - src/turnero.ts — confirm CALL_NEXT exists in QueueAction union (prerequisite from Plan 01)
  </read_first>
  <action>
    Modify src/App.tsx in six precise locations (read the file first — line numbers are approximate and may shift):

    Location 1 — Import line (top of file): Add useEffect to the React import. Change `import { useState, useReducer } from 'react'` to `import { useState, useReducer, useEffect } from 'react'`.

    Location 2 — VentanillaCard function signature: Extend the Readonly<{...}> props to add onCallNext and isQueueEmpty. Keep the same Readonly<{...}> wrapper (SonarLint S6759 — do not remove). Add two new destructured params and two new prop type entries:
    - `onCallNext: (id: number) => void` (mirrors onRemove signature exactly per D-08)
    - `isQueueEmpty: boolean`

    Location 3 — Local state declarations (inside VentanillaCard, after the existing showWarning useState): Add a second local state: `const [showEmptyWarning, setShowEmptyWarning] = useState(false)`.

    Location 4 — useEffect hooks (after the two useState lines): Add two new useEffect hooks in this order:
    First (WR-01 fix — D-10): watches ventanilla.currentTicket; when it changes to null, resets showWarning to false. Dep array MUST be `[ventanilla.currentTicket]` — NOT `[]` (never re-runs) and NOT `[ventanilla]` (changes reference on every parent render). The 04-PATTERNS.md section "WR-01 useEffect" shows the exact code.
    Second (CALL-02 auto-dismiss — D-06): watches showEmptyWarning; when true, sets a 2000ms setTimeout that calls setShowEmptyWarning(false) and returns the cleanup `() => clearTimeout(timer)`. If showEmptyWarning is false, return early with no timer. The 04-PATTERNS.md section "CALL-02 useEffect" shows the exact code.

    Location 5 — Handler functions (after the existing handleRemove function): Add handleCallNext function. Pattern: if isQueueEmpty is true, call setShowEmptyWarning(true) and return early. Otherwise call onCallNext(ventanilla.id). Do NOT call onCallNext on empty queue (D-03: reducer no-ops, but UI feedback is the component's responsibility per D-04).

    Location 6 — JSX return (inside VentanillaCard div): Add the button and second warning. Card layout order per D-07 (top to bottom):
    1. × button (existing, absolute top-right — unchanged)
    2. h3.ventanilla-label (existing — unchanged)
    3. p.ventanilla-ticket (existing — unchanged)
    4. NEW: button.call-next-button with onClick={handleCallNext}, text "Llamar siguiente", type="button"
    5. EXISTING: {showWarning && <p className="ventanilla-warning">No se puede quitar...</p>}
    6. NEW: {showEmptyWarning && <p className="ventanilla-warning">No hay turnos en espera</p>}

    Location 7 — App JSX VentanillaCard invocation: Add two new props to the existing render call:
    - `onCallNext={(id) => dispatch({ type: 'CALL_NEXT', windowId: id })}`
    - `isQueueEmpty={state.queue.length === 0}`
    These go after the existing onRemove prop. The CALL_NEXT action windowId matches the reducer's action.windowId field added in Plan 01.

    Modify src/index.css — one addition:
    After the .ventanilla-warning rule (ends around line 144) and before .ventanillas-empty (starts around line 147): Insert the .call-next-button rule. It follows .add-window-button conventions (same background, color, border, border-radius, padding, font-size, font-weight, cursor) with two additions: `width: 100%` (D-07 full-width) and `margin: 8px 0 0` (spacing below the ticket display). The exact CSS is in 04-PATTERNS.md section "src/index.css".

    After both files are updated, commit: git commit -m "feat(04-02): extend VentanillaCard with Llamar siguiente button and add call-next-button CSS"
  </action>
  <verify>
    <automated>npm test</automated>
    All 21 tests must pass (15 unit in turnero.test.ts + 6 integration in App.test.tsx).
    Exit code must be 0. Any failure is a blocker — do not proceed to the checkpoint.
  </verify>
  <done>
    - VentanillaCard accepts onCallNext: (id: number) => void and isQueueEmpty: boolean props inside Readonly<{...}>
    - useEffect with dep [ventanilla.currentTicket] resets showWarning to false when ticket becomes null (WR-01)
    - useEffect with dep [showEmptyWarning] sets setTimeout(2000) when true and clears it on cleanup (CALL-02)
    - handleCallNext function: calls setShowEmptyWarning(true) when isQueueEmpty, else calls onCallNext(ventanilla.id)
    - JSX: button.call-next-button with text "Llamar siguiente" appears below p.ventanilla-ticket and above the warning paragraphs
    - showEmptyWarning && p.ventanilla-warning showing "No hay turnos en espera" appears below the button
    - App JSX VentanillaCard invocation passes onCallNext and isQueueEmpty={state.queue.length === 0}
    - .call-next-button rule in index.css includes width: 100%
    - npm test exits 0 with 21 tests passing
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 extended VentanillaCard with the "Llamar siguiente" button (full-width, at card bottom), an inline empty-queue warning ("No hay turnos en espera" auto-dismissing after 2 seconds), and wired dispatch to the CALL_NEXT reducer action. All 21 automated tests pass.
  </what-built>
  <how-to-verify>
    Run `npm run dev` to start the Vite dev server, then open http://localhost:5173 in the browser.

    Test A — Button visibility (D-07, D-01):
    1. Add 2 ventanillas using "Agregar ventanilla"
    2. Confirm both cards show a "Llamar siguiente" button at the bottom of the card, below the "sin turno" text, spanning full card width
    3. Confirm the button is present and looks clickable regardless of ticket state

    Test B — Successful call (CALL-01):
    1. Click "Agregar turno" to add ticket 1 to the queue
    2. Click "Agregar turno" to add ticket 2
    3. Click "Llamar siguiente" on Ventanilla 1 — Ventanilla 1 should now show "Turno 1", queue chip "Turno 1" should disappear, queue chip "Turno 2" remains
    4. Click "Llamar siguiente" on Ventanilla 2 — Ventanilla 2 should show "Turno 2", queue becomes empty
    5. Click "Llamar siguiente" on Ventanilla 1 again (replace-always, D-02) — add ticket 3 first; Ventanilla 1 should show "Turno 3", replacing "Turno 1"

    Test C — Empty queue warning (CALL-02):
    1. Ensure queue is empty (no chips visible in Cola section)
    2. Click "Llamar siguiente" on any ventanilla — "No hay turnos en espera" should appear inline inside ONLY that card
    3. Wait 2 seconds — the warning should disappear automatically
    4. Click "Llamar siguiente" again — a second warning should appear (button remains usable after dismiss)

    Test D — WR-01 stale warning regression:
    1. Add a ventanilla and add a ticket
    2. Click "Llamar siguiente" on that ventanilla (assigns ticket)
    3. Click × (remove) on the ventanilla — warning "No se puede quitar: tiene un turno activo" appears
    4. Add another ticket and click "Llamar siguiente" on the same ventanilla — the removal warning must disappear (WR-01 fix)
    5. Now click × again — new removal warning should appear (button still works correctly)
  </how-to-verify>
  <resume-signal>Type "approved" if all four tests pass. Describe any failures so they can be fixed before proceeding to verify-work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → VentanillaCard local state | Click handler reads isQueueEmpty prop and either updates local state or calls parent callback |
| VentanillaCard → App dispatch | onCallNext callback carries ventanilla.id into dispatch({ type: 'CALL_NEXT', windowId: id }) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service (local) | VentanillaCard useEffect — setTimeout auto-dismiss | mitigate | `return () => clearTimeout(timer)` in useEffect cleanup prevents dangling timers on unmount or rapid re-trigger |
| T-04-04 | Tampering | VentanillaCard showWarning local state (WR-01) | mitigate | `useEffect([ventanilla.currentTicket])` resets showWarning to false when prop changes to null — prevents stale "has active ticket" warning after external CALL_NEXT clears the ticket |
| T-04-05 | Tampering | isQueueEmpty prop stale closure | accept | React re-renders VentanillaCard when state.queue changes; stale closure during rapid click is caught by reducer's own no-op guard (CALL_NEXT on empty queue = no-op already mitigated in Plan 01) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; no slopcheck needed |
</threat_model>

<verification>
After Plan 02 completes:
- npm test exits 0 with 21 tests (15 unit + 6 integration)
- grep "call-next-button" src/index.css confirms CSS class exists with width: 100%
- grep "onCallNext" src/App.tsx confirms both the prop declaration and the App-level dispatch wiring
- grep "isQueueEmpty" src/App.tsx confirms both the prop destructuring and the state.queue.length === 0 computation
- grep "ventanilla.currentTicket" src/App.tsx confirms the WR-01 useEffect dep array
- Visual checkpoint: approved by user
- All Phase 4 success criteria verifiable in browser (Test A/B/C/D in checkpoint above)
</verification>

<success_criteria>
- "Llamar siguiente" button visible in every VentanillaCard at all times — before and after calling a ticket
- Clicking the button on a ventanilla when queue has tickets: the ventanilla's display changes to the called ticket number and that ticket chip disappears from the queue strip
- Clicking the button when queue is empty: inline message "No hay turnos en espera" appears, disappears automatically after 2 seconds, button remains clickable
- A ventanilla with an existing ticket can call next — previous ticket is silently replaced (D-02)
- The removal warning does not persist incorrectly after a CALL_NEXT clears the active ticket from outside the component (WR-01)
- All 21 tests (15 unit + 6 integration) pass — npm test exits 0
- Visual checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/04-call-next-atomic-dequeue/04-02-SUMMARY.md` when done
</output>
