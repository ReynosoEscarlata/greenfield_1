---
phase: 04-call-next-atomic-dequeue
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/turnero.ts
  - src/turnero.test.ts
  - src/App.test.tsx
autonomous: true
requirements:
  - CALL-01

must_haves:
  truths:
    - "CALL_NEXT is a member of the QueueAction discriminated union with shape { type: 'CALL_NEXT'; windowId: number }"
    - "CALL_NEXT reducer case dequeues state.queue[0] and assigns it to the matching ventanilla's currentTicket"
    - "CALL_NEXT on an empty queue returns state unchanged (referential equality — same object reference)"
    - "REMOVE_WINDOW on a ventanilla with a non-null currentTicket returns state unchanged (WR-02 guard)"
    - "All 15 unit tests pass after Task 2 (11 existing + 4 new CALL-01/WR-02)"
    - "App.test.tsx integration tests are RED after Task 1 (expected — VentanillaCard UI not yet updated)"
  artifacts:
    - path: "src/turnero.ts"
      provides: "CALL_NEXT in QueueAction union, CALL_NEXT reducer case, WR-02 REMOVE_WINDOW guard"
      contains: "type: 'CALL_NEXT'; windowId: number"
    - path: "src/turnero.test.ts"
      provides: "4 new unit tests: CALL-01-A/B/C and WR-02-A"
      exports: ["describe('CALL-01: CALL_NEXT reducer')", "describe('WR-02: REMOVE_WINDOW reducer guard')"]
    - path: "src/App.test.tsx"
      provides: "3 new integration tests (CALL-01, CALL-02, WR-01) + existing tests updated with new required props"
      contains: "onCallNext={() => {}}"
  key_links:
    - from: "src/turnero.test.ts"
      to: "src/turnero.ts"
      via: "queueReducer(state, { type: 'CALL_NEXT', windowId: 1 })"
      pattern: "CALL_NEXT.*windowId"
    - from: "src/App.test.tsx"
      to: "src/App.tsx"
      via: "VentanillaCard with onCallNext and isQueueEmpty props"
      pattern: "onCallNext.*isQueueEmpty"
---

<objective>
Establish the TDD contract for Phase 4: write all failing tests that define the expected behavior, then implement the reducer changes that make the unit tests pass. Integration tests remain RED after this plan — they go GREEN in Plan 02 when the VentanillaCard UI is extended.

Purpose: The reducer is the authoritative state machine. Landing CALL_NEXT and the WR-02 guard here, verified by unit tests, gives Plan 02 a stable and tested foundation to wire the UI against.
Output: Extended src/turnero.ts (CALL_NEXT + WR-02), extended src/turnero.test.ts (15 passing unit tests), extended src/App.test.tsx (3 new integration tests RED by design + 3 existing tests updated with new props).
</objective>

<execution_context>
@C:/Users/Richi/AppData/Local/nvm/v22.23.0/node_modules/npm/node_modules/@npmcli/run-script/../../
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-CONTEXT.md
</execution_context>

<context>
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/PROJECT.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/ROADMAP.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-PATTERNS.md
@C:/Users/Richi/OneDrive/Desktop/Challenges claude/challenge_2/greenfield_1/.planning/phases/04-call-next-atomic-dequeue/04-RESEARCH.md

<interfaces>
<!-- Key contracts extracted from src/turnero.ts (current state — READ the file before modifying). -->

Current QueueAction union (lines 35-38):
  export type QueueAction =
    | { type: 'ADD_TICKET' }
    | { type: 'ADD_WINDOW' }
    | { type: 'REMOVE_WINDOW'; id: number }
  // Phase 4 extension point: add { type: 'CALL_NEXT'; windowId: number } here.

Current REMOVE_WINDOW case (lines 76-83) — will be REPLACED by WR-02 guard version:
  case 'REMOVE_WINDOW': {
    // Removes unconditionally — WINDOW-02 guard lives in VentanillaCard (UI layer).
    return { ...state, ventanillas: state.ventanillas.filter((v) => v.id !== action.id) }
  }

Ventanilla type (lines 16-20):
  export type Ventanilla = { id: number; number: number; currentTicket: Ticket | null }

<!-- Key contracts extracted from src/App.test.tsx (current state — READ the file before modifying). -->

Existing render calls that MUST have onCallNext and isQueueEmpty added (they will TypeScript-error
once VentanillaCard accepts the new props in Plan 02 if not updated now):
  - WINDOW-03 test: render(<VentanillaCard ventanilla={...} onRemove={() => {}} />)
  - WINDOW-02 test 1: render(<VentanillaCard ventanilla={...} onRemove={onRemove} />)
  - WINDOW-02 test 2: render(<VentanillaCard ventanilla={...} onRemove={onRemove} />)
Each must receive: onCallNext={() => {}} isQueueEmpty={false} (or isQueueEmpty={true} when currentTicket is non-null)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED tests (turnero.test.ts + App.test.tsx) — TDD Red gate</name>
  <files>src/turnero.test.ts, src/App.test.tsx</files>
  <read_first>
    - src/turnero.test.ts — read entire file; new describe blocks append AFTER line 83
    - src/App.test.tsx — read entire file; update existing 3 render calls AND append new describe blocks
    - src/turnero.ts — read to confirm current QueueAction union shape (the new tests reference CALL_NEXT which does not exist yet — that is the RED state)
    - .planning/phases/04-call-next-atomic-dequeue/04-PATTERNS.md — sections "src/turnero.test.ts" and "src/App.test.tsx" contain the exact test code to copy
  </read_first>
  <behavior>
    Unit test expectations (src/turnero.test.ts — append two new describe blocks):
    - CALL-01-A: ADD_WINDOW + ADD_TICKET + CALL_NEXT(windowId:1) → ventanillas[0].currentTicket.number === 1 AND queue.length === 0
    - CALL-01-B: ADD_WINDOW + CALL_NEXT(windowId:1) with empty queue → returned state is referentially identical to input state (expect(state).toBe(before))
    - CALL-01-C: ADD_WINDOW + 2×ADD_TICKET + 2×CALL_NEXT(windowId:1) → ventanillas[0].currentTicket.number === 2 (second ticket replaces first)
    - WR-02-A: ADD_WINDOW + ADD_TICKET + CALL_NEXT(windowId:1) [sets active ticket] + REMOVE_WINDOW(id:1) → returned state is referentially identical (no-op), ventanillas.length === 1

    Integration test expectations (src/App.test.tsx — update existing tests + append new describe blocks):
    - Update all 3 existing render(<VentanillaCard ...>) calls: add onCallNext={() => {}} and isQueueEmpty={false} (keep currentTicket: {id:5,number:5} tests with isQueueEmpty={false})
    - Add import of act from @testing-library/react (extend the existing import line)
    - CALL-01 integration: render VentanillaCard(isQueueEmpty:false, onCallNext:vi.fn()) → fireEvent.click("Llamar siguiente") → expect(onCallNext).toHaveBeenCalledWith(1)
    - CALL-02 show+dismiss: vi.useFakeTimers() in beforeEach, vi.useRealTimers() in afterEach; render VentanillaCard(isQueueEmpty:true) → fireEvent.click("Llamar siguiente") → expect "No hay turnos en espera" in document → act(() => vi.advanceTimersByTime(2000)) → expect text NOT in document
    - WR-01 integration: render VentanillaCard(currentTicket:{id:5,number:5}, isQueueEmpty:false) → click × button → warning visible → rerender with currentTicket:null and isQueueEmpty:true → expect warning NOT in document
  </behavior>
  <action>
    Step 1 — Extend src/turnero.test.ts:
    Append two new describe blocks after the last existing test (after the closing brace of "WINDOW-03" describe, line 83). Use the sequential dispatch pattern `let state = queueReducer(initialState, { type: 'ADD_WINDOW' })` already established in the file.

    Describe block 1: describe('CALL-01: CALL_NEXT reducer') with three it() cases:
    - CALL-01-A: 3 dispatches (ADD_WINDOW, ADD_TICKET, CALL_NEXT windowId:1); assert ventanillas[0].currentTicket?.number toBe(1) and queue toHaveLength(0)
    - CALL-01-B: 2 dispatches (ADD_WINDOW, CALL_NEXT windowId:1 on empty queue); capture state before as `const before = state`; assert expect(state).toBe(before) using referential equality
    - CALL-01-C: 4 dispatches (ADD_WINDOW, ADD_TICKET, ADD_TICKET, CALL_NEXT windowId:1, CALL_NEXT windowId:1); assert ventanillas[0].currentTicket?.number toBe(2)

    Describe block 2: describe('WR-02: REMOVE_WINDOW reducer guard') with one it() case:
    - WR-02-A: 4 dispatches (ADD_WINDOW, ADD_TICKET, CALL_NEXT windowId:1, then capture `const before = state`, then REMOVE_WINDOW id:1); assert expect(state).toBe(before) and ventanillas toHaveLength(1)

    Step 2 — Update src/App.test.tsx:
    Line 1: extend the @testing-library/react import to include act: `import { render, screen, fireEvent, act } from '@testing-library/react'`

    Update the 3 existing render(<VentanillaCard ...>) calls — add the two new required props to each:
    - WINDOW-03 test (currentTicket:null): add `onCallNext={() => {}} isQueueEmpty={false}`
    - WINDOW-02 first test (currentTicket:{id:5,number:5}): add `onCallNext={() => {}} isQueueEmpty={false}`
    - WINDOW-02 second test (currentTicket:null): add `onCallNext={() => {}} isQueueEmpty={false}`

    Append three new describe blocks after the existing tests:
    - describe('CALL-01: Llamar siguiente dispatches CALL_NEXT'): one it() using vi.fn() for onCallNext, render with isQueueEmpty:false, fireEvent.click on button matching /llamar siguiente/i, assert onCallNext toHaveBeenCalledWith(1)
    - describe('CALL-02: Empty-queue warning auto-dismiss'): beforeEach(vi.useFakeTimers), afterEach(vi.useRealTimers); one it() render with isQueueEmpty:true, fireEvent.click, expect "No hay turnos en espera" in document, act(() => vi.advanceTimersByTime(2000)), expect text NOT in document
    - describe('WR-01: showWarning resets when currentTicket is cleared externally'): one it() using rerender; initial render with currentTicket:{id:5,number:5}, click × button, expect warning present; rerender with currentTicket:null and isQueueEmpty:true, expect warning absent

    CRITICAL: Do NOT fenced code blocks inline here. All patterns and exact code are in 04-PATTERNS.md sections "src/turnero.test.ts" and "src/App.test.tsx" — copy those exactly.

    After both files are written, commit as RED gate: git commit -m "test(04-01): add failing CALL-01/CALL-02/WR-01/WR-02 tests (Red phase)"
    Expected result: npm test shows failures (CALL_NEXT does not exist in QueueAction union yet → TypeScript/runtime errors in turnero.test.ts; VentanillaCard missing new props → errors in App.test.tsx). This is the intended RED state.
  </action>
  <verify>
    <automated>npm test -- src/turnero.test.ts 2>&amp;1 | grep -E "(FAIL|Error|Cannot|fail)" | head -5</automated>
    RED confirmed when: the 4 new CALL-01/WR-02 tests show as failures (TypeScript compile error on unknown CALL_NEXT action type or runtime error). The 11 existing unit tests may still pass — that is acceptable for the RED gate.
  </verify>
  <done>
    - src/turnero.test.ts contains two new describe blocks: "CALL-01: CALL_NEXT reducer" (3 it cases) and "WR-02: REMOVE_WINDOW reducer guard" (1 it case)
    - src/App.test.tsx has act imported from @testing-library/react, all 3 existing VentanillaCard renders include onCallNext={() => {}} and isQueueEmpty={false}, and 3 new describe blocks (CALL-01, CALL-02, WR-01) are appended
    - git log shows a RED gate commit with message matching "test(04-01): add failing CALL-01/CALL-02/WR-01/WR-02 tests (Red phase)"
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement reducer — CALL_NEXT case + WR-02 guard (TDD Green gate for unit tests)</name>
  <files>src/turnero.ts</files>
  <read_first>
    - src/turnero.ts — read entire file before editing; the extension point comment is at line 34 and the REMOVE_WINDOW case is at lines 76-83
    - .planning/phases/04-call-next-atomic-dequeue/04-PATTERNS.md — sections "src/turnero.ts" and "Shared Patterns" contain the exact CALL_NEXT case and WR-02 guard code
    - src/turnero.test.ts — scan the new test assertions to confirm the implementation satisfies each expectation exactly
  </read_first>
  <action>
    Modify src/turnero.ts in three precise locations:

    Location 1 — QueueAction union (after REMOVE_WINDOW line 38, before the closing semicolon):
    Add `| { type: 'CALL_NEXT'; windowId: number }` as the fourth member. The comment "Phase 4 extension point: add..." on line 34 marks where to extend.

    Location 2 — REMOVE_WINDOW case (lines 76-83): Replace the unconditional body with the WR-02 guard. Before the filter return, add:
    `const target = state.ventanillas.find((v) => v.id === action.id)`
    `if (target !== undefined && target.currentTicket !== null) { return state }`
    Then the existing spread + filter return. The guard check MUST use `target !== undefined &&` before `target.currentTicket !== null` — do NOT use optional chaining here (see RESEARCH.md Pitfall 4: optional chaining on undefined evaluates to undefined which is !== null, causing a false positive block).

    Location 3 — After REMOVE_WINDOW case, before the `default:` case: Add the CALL_NEXT case:
    `case 'CALL_NEXT': {`
    `  if (state.queue.length === 0) return state`
    `  const [nextTicket, ...remainingQueue] = state.queue`
    `  return {`
    `    ...state,`  (CRITICAL: must spread state first — same discipline as ADD_TICKET fix from Phase 3)
    `    queue: remainingQueue,`
    `    ventanillas: state.ventanillas.map((v) =>`
    `      v.id === action.windowId ? { ...v, currentTicket: nextTicket } : v`
    `    ),`
    `  }`
    `}`

    After implementing, commit as GREEN gate: git commit -m "feat(04-01): extend turnero.ts with CALL_NEXT action and WR-02 reducer guard"
  </action>
  <verify>
    <automated>npm test -- src/turnero.test.ts</automated>
    All 15 tests must pass (11 existing + 4 new: CALL-01-A, CALL-01-B, CALL-01-C, WR-02-A).
    App.test.tsx integration tests remain RED at this point — that is expected and correct.
  </verify>
  <done>
    - src/turnero.ts QueueAction union has 4 members including { type: 'CALL_NEXT'; windowId: number }
    - REMOVE_WINDOW case has the WR-02 guard (find + target !== undefined + currentTicket !== null check before the filter return)
    - CALL_NEXT case exists after REMOVE_WINDOW, uses ...state spread, destructures [nextTicket, ...remainingQueue] from state.queue, maps ventanillas to update the matching window's currentTicket
    - npm test -- src/turnero.test.ts exits 0 with 15 tests passing
    - git log shows GREEN gate commit with message matching "feat(04-01): extend turnero.ts with CALL_NEXT action and WR-02 reducer guard"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch → queueReducer | Reducer receives actions from UI event handlers; must not trust call sites to enforce guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | queueReducer REMOVE_WINDOW case | mitigate | WR-02 guard: `if (target !== undefined && target.currentTicket !== null) return state` — prevents silent active-ticket loss via any dispatch path, not just UI |
| T-04-02 | Tampering | queueReducer CALL_NEXT case (windowId not found) | accept | `.map` with id-not-found returns same ventanillas array with no changes — structurally safe no-op; no PII at risk |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; no slopcheck needed |
</threat_model>

<verification>
After Plan 01 completes:
- npm test -- src/turnero.test.ts → 15/15 passing (GREEN gate confirmed)
- npm test -- src/App.test.tsx → failures expected (RED by design — VentanillaCard not yet extended)
- git log --oneline -3 shows both TDD commits: RED gate then GREEN gate in order
- grep "CALL_NEXT" src/turnero.ts confirms the union member and reducer case both exist
- grep "target !== undefined" src/turnero.ts confirms WR-02 guard uses explicit undefined check (not optional chaining)
</verification>

<success_criteria>
- CALL_NEXT is a valid member of QueueAction — TypeScript accepts { type: 'CALL_NEXT', windowId: 1 } without error
- CALL_NEXT dequeues head: state after dispatch has queue.length reduced by 1 and matching ventanilla.currentTicket set to the former head ticket
- CALL_NEXT on empty queue returns the same state object reference (React skips re-render)
- REMOVE_WINDOW on a ventanilla with active currentTicket returns the same state object reference
- All 15 unit tests pass; App.test.tsx integration tests are failing (held for Plan 02)
- TDD Red/Green commit sequence present in git log (D-12 compliance)
</success_criteria>

<output>
Create `.planning/phases/04-call-next-atomic-dequeue/04-01-SUMMARY.md` when done
</output>
