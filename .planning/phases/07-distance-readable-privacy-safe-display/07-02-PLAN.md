---
phase: 07-distance-readable-privacy-safe-display
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/index.css
autonomous: false
requirements:
  - DISPLAY-01
must_haves:
  truths:
    - "Ticket numbers are legible from approximately 3 meters within 5 seconds"
    - "The ventanilla grid wraps to a second row when 4+ ventanillas are added (max 3 per row on ~1136px content width)"
    - "Ticket number and ventanilla label are horizontally centered inside each card"
    - "Flash animation bounding box is visually proportionate at 48px ticket text size"
    - "Queue chips in the Cola strip are visibly larger than typical body text"
  artifacts:
    - path: "src/index.css"
      provides: "Visually confirmed CSS — all D-01 through D-05 changes render correctly in browser"
      contains: "font-size: 48px"
  key_links:
    - from: "browser rendering at 100% zoom"
      to: "src/index.css .ventanilla-ticket { font-size: 48px; font-weight: 700 }"
      via: "visual inspection at ~3m viewing distance"
      pattern: "ticket number readable at 3m in under 5 seconds"
---

## Phase Goal

**As a** waiting-room patient or operator, **I want to** read ticket numbers from across the room and trust that no personal data is shown, **so that** the screen functions as a real waiting-room display board.

<objective>
Visual verification checkpoint: confirm that the CSS changes from Plan 07-01 produce a genuinely distance-readable, correctly laid-out display in the browser — and optionally fix .ventanilla-ticket-flash border-radius if the flash area looks disproportionately tight at 48px.

Purpose: DISPLAY-01 cannot be auto-verified by jsdom (no layout engine — jsdom does not compute rendered pixel sizes). This checkpoint gates phase completion on real-browser visual confirmation.

Output: Human confirmation that ticket numbers are readable at 3m; OR a corrected .ventanilla-ticket-flash border-radius value applied to src/index.css if the flash area looked proportionally wrong on the larger text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-distance-readable-privacy-safe-display/07-01-SUMMARY.md
@.planning/phases/07-distance-readable-privacy-safe-display/07-UI-SPEC.md
@.planning/phases/07-distance-readable-privacy-safe-display/07-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification — distance readability, grid layout, centering, and flash proportionality</name>
  <files>src/index.css</files>
  <read_first>
    .planning/phases/07-distance-readable-privacy-safe-display/07-01-SUMMARY.md — confirm Plan 07-01 completed and npm test passed before proceeding.
    src/index.css — read .ventanilla-ticket-flash (near line 133) to confirm current border-radius value (4px) before considering an adjustment.
  </read_first>
  <what-built>
    Plan 07-01 applied 10 property edits to src/index.css:
    - .ventanilla-ticket: font-size 48px / font-weight 700 (was 16px / 400) — D-01
    - .ventanilla-label: font-size 22px / font-weight 700 (was 16px / 600) — D-02
    - .ticket-chip: font-size 22px / font-weight 400 (was 16px / 400) — D-03
    - .ventanillas-grid: repeat(auto-fill, minmax(350px, 1fr)) (was repeat(auto-fit, minmax(200px, 1fr))) — D-04
    - .ventanilla-card: added text-align: center — D-05
    - .queue-strip h2 / .ventanillas-section h2: 22px / 700 (was 20px / 600) — UI-SPEC consolidation
    - .page-title: font-weight 700 (was 600) — UI-SPEC consolidation
    - .add-ticket-button, .add-window-button, .call-next-button: font-weight 400 (was 600) — UI-SPEC consolidation
    Plan 07-01 also added a PRIVACY-01 describe block (2 passing tests) to src/App.test.tsx.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the Vite dev server.
    2. Open http://localhost:5173 in a browser at 100% zoom (default zoom, no scaling override).
    3. Grid layout check: click "Agregar ventanilla" four times to add 4 cards. Confirm that the 4th card wraps to a second row — exactly 3 cards appear on the first row. If all 4 cards appear side-by-side on one row, the minmax(350px) constraint is not applying.
    4. Queue chip size check: click "Agregar turno" three times to add 3 chips. Confirm the chips in the "Cola" section look noticeably larger than small body text (target: ~22px, approximately 1.4x the size of the 16px empty-state text beside them).
    5. Distance readability check: click "Llamar siguiente" on one ventanilla. Confirm the ticket number (e.g., "Turno 1") appears large and bold on the card. Step approximately 3 meters back from the screen and read the ticket number within 5 seconds. It must be clearly legible without straining.
    6. Centering check: confirm the ventanilla label ("Ventanilla 1") and the ticket number are centered horizontally within the card at all times.
    7. Flash animation check: immediately after clicking "Llamar siguiente", observe the yellow highlight flash over the ticket number. Confirm the yellow bounding area looks proportionate to the 48px text — not a tiny hairline rectangle squeezed around large text. The current border-radius is 4px; if it looks too tight on the larger element, increase it.
    8. If the flash border-radius needs adjustment: edit .ventanilla-ticket-flash in src/index.css, changing border-radius from 4px to 8px. Run `npm test` after the change to confirm no regressions.
  </how-to-verify>
  <verify>
    <human-check>
      All visual checks above pass. If flash border-radius was adjusted to 8px: npm test exits 0 after the change.
    </human-check>
  </verify>
  <acceptance_criteria>
    - 4th ventanilla card wraps to a second row (only 3 cards on first row)
    - Ticket number at 48px/700 is readable from approximately 3 meters without effort
    - Queue chips are visibly larger than the 16px empty-state text
    - Ventanilla label and ticket number are centered horizontally in every card
    - Flash animation plays when a ticket is called and its bounding area looks proportionate to the 48px text
    - If border-radius was adjusted: .ventanilla-ticket-flash border-radius is now 8px in src/index.css, and `npm test` exits 0
    - No unexpected content (patient names, identifiers) is visible on screen (PRIVACY-01 visual confirm)
  </acceptance_criteria>
  <resume-signal>
    Type "approved" if all visual checks pass and no border-radius adjustment was needed.
    Type "approved — flash adjusted border-radius to 8px" if you updated .ventanilla-ticket-flash.
    Describe any unexpected layout issue if found (e.g., "4 cards on one row — grid not constraining").
  </resume-signal>
  <done>DISPLAY-01 visually confirmed: ticket numbers legible at 3 meters; grid enforces max 3 per row; numbers centered; flash animation proportionate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser render -> human visual check | jsdom cannot verify pixel-level layout; human must confirm rendered appearance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | Browser display during visual checkpoint | accept | Human verifier confirms no patient names or identifiers appear on screen; PRIVACY-01 automated tests in Plan 07-01 already enforce this structurally |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; package manager is not invoked |
</threat_model>

<verification>
Manual verification only for DISPLAY-01 (jsdom has no layout engine):
- Ticket numbers legible at approximately 3 meters in under 5 seconds
- Max 3 ventanilla cards per row on the ~1136px content area
- Numbers and labels centered in cards
- Flash animation visually proportionate at 48px text size
If border-radius was adjusted during checkpoint:
  grep "border-radius" src/index.css   # .ventanilla-ticket-flash must show 8px
  npm test                              # must exit 0
</verification>

<success_criteria>
DISPLAY-01 is visually confirmed: the waiting-room screen shows ticket numbers at 48px/700, max 3 cards per row, ticket numbers centered, and the flash animation is proportionate. A real patient or operator can read the current ticket number from across the room within 5 seconds.
</success_criteria>

<output>
Create `.planning/phases/07-distance-readable-privacy-safe-display/07-02-SUMMARY.md` when done.
</output>
