---
phase: 07-distance-readable-privacy-safe-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/App.test.tsx
  - .planning/phases/07-distance-readable-privacy-safe-display/VERIFICATION.md
autonomous: true
requirements:
  - DISPLAY-01
  - PRIVACY-01
must_haves:
  truths:
    - "`.ventanilla-ticket` has `font-size: 48px` and `font-weight: 700` — primary ticket number is distance-readable at 3m"
    - "`.ventanillas-grid` uses `grid-template-columns: repeat(auto-fill, minmax(350px, 1fr))` — enforces max 3 cards per row on 1200px container"
    - "`.ventanilla-card` has `text-align: center` — ticket number and label are horizontally centered"
    - "`.ticket-chip` has `font-size: 22px` — queue chips are scannable from distance"
    - "No `font-weight: 600` declaration remains anywhere in src/index.css (2-weight consolidation complete)"
    - "`npm test` exits 0 with 2 new PRIVACY-01 tests confirming VentanillaCard renders only ticket integers, never patient identifiers"
  artifacts:
    - path: "src/index.css"
      provides: "Distance-readable CSS rules — 10 targeted property edits"
      contains: "font-size: 48px"
    - path: "src/App.test.tsx"
      provides: "PRIVACY-01 automated regression tests"
      contains: "PRIVACY-01: No patient-identifying data rendered"
  key_links:
    - from: "src/index.css .ventanilla-ticket"
      to: "VentanillaCard <p className=\"ventanilla-ticket\"> in App.tsx (line 65)"
      via: "CSS class selector applied to JSX element"
      pattern: "font-size: 48px"
    - from: "src/index.css .ventanillas-grid"
      to: "App.tsx <div className=\"ventanillas-grid\"> (line 127)"
      via: "CSS class selector"
      pattern: "minmax\\(350px"
---

## Phase Goal

**As a** waiting-room patient or operator, **I want to** read ticket numbers from across the room and trust that no personal data is shown, **so that** the screen functions as a real waiting-room display board.

<objective>
Apply all typography scaling, grid layout, and 2-weight consolidation CSS edits to src/index.css (per D-01 through D-05 and the approved UI-SPEC), then append the PRIVACY-01 automated test suite to src/App.test.tsx.

Purpose: Make ticket numbers distance-readable (48px / 700) on the waiting-room display and formally verify through automated tests that no patient-identifying data is ever rendered.

Output: src/index.css updated with 10 targeted property edits; src/App.test.tsx extended with a PRIVACY-01 describe block (2 tests); all existing tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-distance-readable-privacy-safe-display/07-CONTEXT.md
@.planning/phases/07-distance-readable-privacy-safe-display/07-RESEARCH.md
@.planning/phases/07-distance-readable-privacy-safe-display/07-UI-SPEC.md

<interfaces>
<!-- Extracted from src/index.css — current selector values BEFORE this plan's edits. -->
<!-- Executor: verify these match what you read from the file, then apply the edits. -->

src/index.css current state (relevant selectors only):

  .page-title (line 18)
      font-size: 28px        -- size stays
      font-weight: 600       -- CHANGE to 700

  .queue-strip h2, .ventanillas-section h2 (line 32)
      font-size: 20px        -- CHANGE to 22px
      font-weight: 600       -- CHANGE to 700

  .ventanillas-grid (line 49)
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr))   -- CHANGE to repeat(auto-fill, minmax(350px, 1fr))

  .add-ticket-button (line 60)
      font-weight: 600       -- CHANGE to 400

  .ticket-chip (line 81)
      font-size: 16px        -- CHANGE to 22px
      font-weight: 400       -- stays 400

  .add-window-button (line 94)
      font-weight: 600       -- CHANGE to 400

  .ventanilla-card (line 106)
      position: relative     -- stays
      (no text-align)        -- ADD text-align: center

  .ventanilla-label (line 110)
      font-size: 16px        -- CHANGE to 22px
      font-weight: 600       -- CHANGE to 700

  .ventanilla-ticket (line 117)
      font-size: 16px        -- CHANGE to 48px
      font-weight: 400       -- CHANGE to 700

  .ventanilla-ticket-flash (line 133)
      animation: ...         -- DO NOT touch font-size here; font-size lives on .ventanilla-ticket
      border-radius: 4px     -- stays (Plan 07-02 visual checkpoint may adjust if needed)

  .call-next-button (line 161)
      font-weight: 600       -- CHANGE to 400

<!-- Extracted from src/App.tsx — key facts for the aria-label audit and PRIVACY-01 tests -->

VentanillaCard props (App.tsx line 6-16):
  ventanilla: Ventanilla { id: number; number: number; currentTicket: Ticket | null }
  onRemove: (id: number) => void
  onCallNext: (id: number) => void
  queueLength: number

Remove button (App.tsx line 55-61) -- already has:
  aria-label={`Quitar ventanilla ${ventanilla.number}`}
  -- THIS IS ALREADY PRESENT. Do NOT change it.
  -- Existing tests use getByRole('button', { name: 'Quitar ventanilla 1' })
  -- and getByRole('button', { name: 'Quitar ventanilla 2' }) -- changing to static
  -- "Quitar ventanilla" would break those tests.

Ticket type (from turnero.ts):
  interface Ticket { id: number; number: number }

App.test.tsx current last line: 203 (closing brace of FEEDBACK-02 describe block)
Vitest globals (vi, describe, it, expect, beforeEach, afterEach) are available without imports.
render and screen are already imported at line 7.
VentanillaCard is already imported at line 8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 10 CSS property edits to src/index.css (D-01 through D-05 + typography consolidation)</name>
  <files>src/index.css</files>
  <read_first>
    src/index.css — read the full file before editing. Confirm the current values match those listed in the interfaces block above. The file has 181 lines; all target selectors are present.
  </read_first>
  <action>
    Edit src/index.css in-place, changing only the listed property values. Do not add or delete selectors, do not change any other properties.

    Edit 1 — .page-title (near line 18):
    Change font-weight from 600 to 700. Font-size 28px stays unchanged.

    Edit 2 — .queue-strip h2, .ventanillas-section h2 (near line 32):
    Change font-size from 20px to 22px. Change font-weight from 600 to 700.

    Edit 3 — .ventanillas-grid (near line 49):
    Change grid-template-columns from `repeat(auto-fit, minmax(200px, 1fr))` to `repeat(auto-fill, minmax(350px, 1fr))`. Use `auto-fill` (not `auto-fit`) so that a single-ventanilla row does not stretch the card to full container width.

    Edit 4 — .add-ticket-button (near line 60):
    Change font-weight from 600 to 400. All other properties unchanged.

    Edit 5 — .ticket-chip (near line 81):
    Change font-size from 16px to 22px. Leave font-weight: 400 unchanged (per D-03 — queue chips stay regular weight).

    Edit 6 — .add-window-button (near line 94):
    Change font-weight from 600 to 400. All other properties unchanged.

    Edit 7 — .ventanilla-card (near line 106):
    Add `text-align: center` to the rule block alongside `position: relative`. The .ventanilla-remove button uses `position: absolute` and is not affected by text-align on the parent.

    Edit 8 — .ventanilla-label (near line 110):
    Change font-size from 16px to 22px. Change font-weight from 600 to 700 (per D-02 and 2-weight consolidation). Margin `0 0 4px` stays unchanged.

    Edit 9 — .ventanilla-ticket (near line 117):
    Change font-size from 16px to 48px. Change font-weight from 400 to 700 (per D-01). Color #1f2933 and margin: 0 stay unchanged. Do NOT set font-size on .ventanilla-ticket-flash — the flash class only adds animation and border-radius; font-size belongs on .ventanilla-ticket so it applies in both flash and non-flash states.

    Edit 10 — .call-next-button (near line 161):
    Change font-weight from 600 to 400. All other properties unchanged.

    After all 10 edits: scan the file for any remaining `font-weight: 600` declarations. There must be zero. The only font-weight values in the file after this edit are 400 (regular) and 700 (bold).
  </action>
  <verify>
    <automated>npm test</automated>
    Run these checks after npm test passes:
    grep -c "font-size: 48px" src/index.css
    grep -c "font-weight: 600" src/index.css
    grep "grid-template-columns" src/index.css
    grep "text-align: center" src/index.css
  </verify>
  <acceptance_criteria>
    - src/index.css contains exactly 1 line matching `font-size: 48px` (on .ventanilla-ticket)
    - src/index.css contains 0 lines matching `font-weight: 600`
    - src/index.css .ventanillas-grid rule contains `repeat(auto-fill, minmax(350px, 1fr))`
    - src/index.css .ventanilla-card rule contains `text-align: center`
    - src/index.css .ventanilla-ticket-flash rule does NOT set font-size
    - `npm test` exits 0 — no regressions from CSS-only changes
  </acceptance_criteria>
  <done>All 10 CSS property edits applied; no font-weight: 600 remains in src/index.css; npm test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Audit aria-label in App.tsx and append PRIVACY-01 test block to App.test.tsx</name>
  <files>src/App.tsx, src/App.test.tsx</files>
  <read_first>
    src/App.tsx — confirm the remove button (lines 55-61) already has aria-label. Current value: `aria-label={\`Quitar ventanilla ${ventanilla.number}\`}`. No edit is needed.
    src/App.test.tsx — read lines 195-203 (end of FEEDBACK-02 describe block) to find the exact insertion point.
  </read_first>
  <action>
    Step 1 — Audit App.tsx (read-only, no edit):
    Read lines 55-61 of App.tsx and confirm the remove button has `aria-label={\`Quitar ventanilla ${ventanilla.number}\`}`. This dynamic value already satisfies the accessibility requirement from the UI-SPEC. Do NOT change it to the static string "Quitar ventanilla" — existing tests use `getByRole('button', { name: 'Quitar ventanilla 1' })` and would fail if the aria-label were changed. No modification to App.tsx is required.

    Step 2 — Append PRIVACY-01 tests to src/App.test.tsx:
    Append a new describe block after the final closing brace of the `FEEDBACK-02: Flash class on ticket change` describe block (currently the last block in the file, ending around line 202). The new block must be named exactly `PRIVACY-01: No patient-identifying data rendered` and must contain two it cases:

    Test case 1 — active ticket state: Render VentanillaCard with ventanilla `{ id: 1, number: 1, currentTicket: { id: 42, number: 42 } }`, onRemove as a no-op arrow function, onCallNext as a no-op arrow function, queueLength as 0. Assert that `screen.getByText('Turno 42')` is in the document. Assert that `screen.queryByTestId('patient-name')` is NOT in the document.

    Test case 2 — empty ticket state: Render VentanillaCard with ventanilla `{ id: 1, number: 1, currentTicket: null }`, onRemove as a no-op arrow function, onCallNext as a no-op arrow function, queueLength as 0. Assert that `screen.getByText('sin turno')` is in the document. Assert that `screen.queryByTestId('patient-name')` is NOT in the document.

    Use the `render` and `screen` identifiers already imported at the top of App.test.tsx. Use Vitest globals (`describe`, `it`, `expect`) without any new import statements. Both tests will pass immediately — PRIVACY-01 is satisfied by design; these tests serve as automated regression guards.

    Step 3 — Create VERIFICATION.md (D-06):
    Create `.planning/phases/07-distance-readable-privacy-safe-display/VERIFICATION.md` with the following structure:
    - Status line: `status: PRIVACY-01 SATISFIED`
    - A "State Shape Audit" section listing the Ticket, Ventanilla, QueueState, and QueueAction types with their fields and confirming each has no patient data field
    - A "Render Audit" section listing every rendered element (queue chip, ventanilla label, ventanilla ticket active/empty, warnings, page title, section headings) and confirming each renders only integers or static strings — never a name field
    - An "Audit Conclusion" section stating: PRIVACY-01 is satisfied by design. No name field exists in any state type definition or action type. No free-text input exists in App.tsx. The screen renders only ticket numbers (integers), ventanilla numbers (integers), and static UI strings.
    The content is drawn directly from RESEARCH.md §Privacy Audit Findings — copy the audit tables verbatim.
  </action>
  <verify>
    <automated>npm test</automated>
    Additional checks after npm test passes:
    grep -c "PRIVACY-01" src/App.test.tsx
    grep -c "patient-name" src/App.test.tsx
  </verify>
  <acceptance_criteria>
    - src/App.tsx remove button retains `aria-label={\`Quitar ventanilla ${ventanilla.number}\`}` — file is unchanged
    - src/App.test.tsx contains a describe block named `PRIVACY-01: No patient-identifying data rendered` with exactly 2 it blocks
    - First it case asserts `screen.getByText('Turno 42')` is in document and `screen.queryByTestId('patient-name')` is not
    - Second it case asserts `screen.getByText('sin turno')` is in document and `screen.queryByTestId('patient-name')` is not
    - Both PRIVACY-01 tests pass (tests confirm existing behavior — no RED state expected)
    - All prior tests continue to pass (no regressions from test file append)
    - `npm test` exits 0 with all tests green
    - `grep -c "PRIVACY-01" src/App.test.tsx` outputs at least 1
    - `grep -c "patient-name" src/App.test.tsx` outputs 2
    - `.planning/phases/07-distance-readable-privacy-safe-display/VERIFICATION.md` exists and contains `status: PRIVACY-01 SATISFIED`
  </acceptance_criteria>
  <done>aria-label confirmed present and unchanged in App.tsx; PRIVACY-01 describe block with 2 green tests appended to App.test.tsx; VERIFICATION.md created with audit conclusion; npm test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public waiting-room screen -> viewer | Screen is visible to all patients; must never render patient names or identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | VentanillaCard JSX render paths | mitigate | PRIVACY-01 describe block in App.test.tsx asserts `queryByTestId('patient-name')` returns null in both active-ticket and empty-ticket states; the root cause is structural — `Ticket { id: number; number: number }` has no name field, so only integers can be rendered |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase; package manager is not invoked; slopcheck not required |
</threat_model>

<verification>
Run after both tasks complete:

  npm test                                         # full suite must exit 0
  grep -c "font-size: 48px" src/index.css         # must output 1
  grep -c "font-weight: 600" src/index.css        # must output 0
  grep "grid-template-columns" src/index.css      # must show: repeat(auto-fill, minmax(350px, 1fr))
  grep "text-align: center" src/index.css         # must output at least 1 match
  grep -c "PRIVACY-01" src/App.test.tsx           # must output at least 1
  grep -c "patient-name" src/App.test.tsx         # must output 2
</verification>

<success_criteria>
- src/index.css has all 10 property edits: .ventanilla-ticket 48px/700, .ventanilla-label 22px/700, .ticket-chip 22px/400, .ventanillas-grid repeat(auto-fill, minmax(350px, 1fr)), .ventanilla-card text-align: center, h2 elements 22px/700, .page-title 700, all three action buttons 400
- No font-weight: 600 remains anywhere in src/index.css
- src/App.test.tsx has PRIVACY-01 describe block with 2 passing tests
- .planning/phases/07-distance-readable-privacy-safe-display/VERIFICATION.md exists with `status: PRIVACY-01 SATISFIED` and state shape + render audit tables (D-06)
- npm test exits 0 with the full test suite green
</success_criteria>

<output>
Create `.planning/phases/07-distance-readable-privacy-safe-display/07-01-SUMMARY.md` when done.
</output>
