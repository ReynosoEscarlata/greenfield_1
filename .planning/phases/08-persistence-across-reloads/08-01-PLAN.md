---
phase: 08-persistence-across-reloads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.test.tsx
autonomous: true
requirements:
  - PERSIST-01
must_haves:
  truths:
    - "App.test.tsx contains a describe('PERSIST-01: Persistence across reloads') block with 5 test cases"
    - "Test suite runs without syntax errors after additions"
    - "SC-1 (hydration test) fails — RED state confirmed — because App.tsx does not yet read localStorage"
    - "Save test fails — RED state confirmed — because App.tsx does not yet write localStorage"
  artifacts:
    - path: "src/App.test.tsx"
      provides: "PERSIST-01 test block — 5 test cases for SC-1, SC-2a, SC-2b, SC-3, and save"
      contains: "describe('PERSIST-01: Persistence across reloads'"
  key_links:
    - from: "App.test.tsx PERSIST-01 describe block"
      to: "App default export"
      via: "import App from './App'"
      pattern: "import App from './App'"
    - from: "PERSIST-01 SC-1 test"
      to: "localStorage.setItem('turnero-v1', ...)"
      via: "pre-populate before render"
      pattern: "localStorage\\.setItem\\('turnero-v1'"
---

## Phase Goal

**As a** clinic receptionist, **I want** the queue and ventanilla state to survive a page reload, **so that** I can refresh the browser without losing the current waiting list.

<objective>
Write the PERSIST-01 test block in App.test.tsx before implementing persistence — establishing the RED state that Plan 08-02 must make green.

Purpose: TDD RED phase — tests exist and fail because App.tsx has no localStorage integration yet. Confirms that test infrastructure (jsdom localStorage, mockPlay spy) works correctly before touching the implementation.
Output: 5 new failing tests in App.test.tsx under describe('PERSIST-01: Persistence across reloads').
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/turnero.ts:
  export type Ticket = { id: number; number: number }
  export type Ventanilla = { id: number; number: number; currentTicket: Ticket | null }
  export type QueueState = { queue: Ticket[]; nextNumber: number; ventanillas: Ventanilla[]; nextWindowNumber: number }
  export const initialState: QueueState

From src/App.tsx (current exports):
  export function VentanillaCard({ ventanilla, onRemove, onCallNext, queueLength })
  export default App
  -- Empty-queue display text (line 107): 'Próximos turnos aparecerán aquí'

From src/App.test.tsx (current top-of-file structure — must not be disturbed):
  Line 1:  const mockPlay = vi.fn()
  Line 3:  vi.mock('./useBeep', () => ({ useBeep: () => ({ play: mockPlay }) }))
  Line 7:  import { render, screen, fireEvent, act } from '@testing-library/react'
  Line 8:  import { VentanillaCard } from './App'
  (File ends after PRIVACY-01 block, current line 231)

Pattern from FEEDBACK-01 describe block (lines 124-154):
  beforeEach(() => { mockPlay.mockClear() })  -- replicate this inside PERSIST-01
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PERSIST-01 test block to App.test.tsx (RED)</name>
  <files>src/App.test.tsx</files>
  <read_first>
    - src/App.test.tsx — read entire file; note import ordering, mock setup, and where each existing describe block ends so additions land in the right place
    - src/turnero.ts — confirm exact QueueState field names (queue, nextNumber, ventanillas, nextWindowNumber) and Ticket/Ventanilla field names for test fixture objects
    - src/App.tsx — confirm the default export is `export default App` and the exact empty-queue text string is 'Próximos turnos aparecerán aquí'
  </read_first>
  <action>
    Step 1 — Add two new imports to src/App.test.tsx, immediately after the existing
    `import { VentanillaCard } from './App'` line (currently line 8):
      import App from './App'
      import type { QueueState } from './turnero'

    Step 2 — Append a new describe block after the closing brace of the PRIVACY-01 block
    (currently the last block in the file). Name: 'PERSIST-01: Persistence across reloads'.

    Step 3 — Inside the PERSIST-01 describe block, add a beforeEach with two calls:
      localStorage.clear()    -- prevents cross-test storage pollution (jsdom localStorage is shared within a file)
      mockPlay.mockClear()    -- resets beep spy between tests

    Step 4 — Add these 5 it() test cases in order inside the PERSIST-01 describe block:

    Case SC-1 (name: 'SC-1: renders persisted queue and ventanilla state after reload'):
      - Declare: const persisted: QueueState = { queue: [{id:2, number:2}, {id:3, number:3}], nextNumber: 4, ventanillas: [{id:1, number:1, currentTicket: {id:1, number:1}}], nextWindowNumber: 2 }
      - Call: localStorage.setItem('turnero-v1', JSON.stringify(persisted))
      - Call: render(<App />)
      - Assert: screen.getByText('Turno 2') toBeInTheDocument  (queue chip)
      - Assert: screen.getByText('Turno 3') toBeInTheDocument  (queue chip)
      - Assert: screen.getByText('Turno 1') toBeInTheDocument  (ventanilla's current ticket display)
      - Assert: screen.getByText('Ventanilla 1') toBeInTheDocument

    Case SC-2a (name: 'SC-2a: falls back to empty state when localStorage key is absent'):
      - No pre-population (localStorage cleared by beforeEach)
      - Call: render(<App />)
      - Assert: screen.getByText('Próximos turnos aparecerán aquí') toBeInTheDocument
      - Assert: screen.queryByText(/^Turno \d/) NOT toBeInTheDocument

    Case SC-2b (name: 'SC-2b: falls back to empty state on corrupted localStorage data'):
      - Call: localStorage.setItem('turnero-v1', 'not-valid-json{{{')
      - Call: render(<App />)
      - Assert: screen.getByText('Próximos turnos aparecerán aquí') toBeInTheDocument

    Case SC-3 (name: 'SC-3: does not call playBeep on initial render with persisted non-null ticket'):
      - Declare: const persisted: QueueState = { queue: [], nextNumber: 2, ventanillas: [{id:1, number:1, currentTicket: {id:1, number:1}}], nextWindowNumber: 2 }
      - Call: localStorage.setItem('turnero-v1', JSON.stringify(persisted))
      - Call: render(<App />)
      - Assert: expect(mockPlay).not.toHaveBeenCalled()
      - CRITICAL: Do NOT assert absence of 'ventanilla-ticket-flash' class on initial render with
        non-null currentTicket — that class IS present by Phase 6 design. Pitfall 5 from RESEARCH.md.

    Case save (name: 'saves updated state to localStorage after dispatch'):
      - Call: render(<App />)
      - Wrap fireEvent.click(screen.getByRole('button', { name: /agregar turno/i })) inside act()
      - Declare: const saved = JSON.parse(localStorage.getItem('turnero-v1') ?? 'null') as QueueState | null
      - Assert: expect(saved).not.toBeNull()
      - Assert: expect(saved!.queue).toHaveLength(1)
      - Assert: expect(saved!.nextNumber).toBe(2)
  </action>
  <verify>
    <automated>grep -c "PERSIST-01: Persistence across reloads" src/App.test.tsx && npm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 1 (the describe block exists exactly once in the file)
    - npm test output includes the 5 new test names (SC-1, SC-2a, SC-2b, SC-3, saves updated state)
    - npm test exits non-zero: SC-1 fails (App does not hydrate from localStorage) and save test fails (no useEffect writes localStorage) — this is the expected RED state
    - SC-2a, SC-2b, and SC-3 may pass incidentally because App currently starts with initialState — this is acceptable; RED gate requires SC-1 and save test to fail
    - All 28 pre-existing tests remain green (import additions must not break existing test suite)
    - No TypeScript errors emitted by the test runner
  </acceptance_criteria>
  <done>App.test.tsx contains the PERSIST-01 describe block with 5 tests and beforeEach. At least SC-1 and save test fail. RED state confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → App component | Test pre-populates jsdom localStorage before rendering App — simulates reload with pre-existing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | localStorage → App hydration | mitigate | loadFromStorage() try/catch (D-01/D-03) — implemented in Plan 08-02; this plan only adds tests that verify the mitigation |
| T-08-02 | Information Disclosure | localStorage data | accept | No PII stored — ticket numbers only (PRIVACY-01 guarantee). Tampering affects only the tamper's own session. |
</threat_model>

<verification>
- grep -c "PERSIST-01: Persistence across reloads" src/App.test.tsx returns 1
- npm test output includes 5 new PERSIST-01 test names
- npm test exits non-zero: SC-1 and save test fail (RED confirmed)
- 28 pre-existing tests remain green
</verification>

<success_criteria>
- src/App.test.tsx has the PERSIST-01 describe block with 5 test cases and a beforeEach
- Imports for App default export and QueueState type are present
- npm test exits non-zero with SC-1 and save test failing (RED state confirmed)
- All 28 pre-existing tests remain green
</success_criteria>

<output>
Create `.planning/phases/08-persistence-across-reloads/08-01-SUMMARY.md` when done
</output>
